# v0.4 UI 打磨十一项 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 按 `docs/superpowers/specs/2026-07-31-poet-map-v04-design.md` 完成 11 项 UI 改进：小地图收拢、其人宽度、地图彩蛋删除、名句彩蛋打磨、字体（白居易文楷/唐朝马善政/导航锁字）、宋体收敛、首页排序与 hover、时间轴定制滑块+粒子、作品时年与卷一起始、作品集渐入与展卷。

**Architecture:** 纯前端改动。字体键（CalligraphyKind）贯穿 zod schema → dynasties.json → 前端 DynastyInfo → 导航内联字体栈；小地图显隐改为「Hero 出视口 && 年表区在视口」双条件；TimeSlider 由原生 input range 重写为自定义 div 滑块（role=slider）；其余为 CSS/参数级改动。

**Tech Stack:** Vite 8 + React 19 + TS 6 + Tailwind 3 + vitest 4 + zod 4；无新增依赖。

## Global Constraints

- 验证命令一律直调二进制（无 TTY 环境 pnpm 会误清 node_modules）：`./node_modules/.bin/tsx scripts/build-data.ts`、`./node_modules/.bin/vitest run`、`./node_modules/.bin/tsc -b`、`./node_modules/.bin/oxlint`
- **跑 vitest 前必须先跑 `./node_modules/.bin/tsx scripts/build-data.ts`**（PoetPage.test 与新增的 build-output.test 读真实 public/data 产物）
- commit message 结尾带 `Co-Authored-By: Claude <noreply@anthropic.com>`；**禁止 push**（需用户确认）
- D10：组件内不得出现 `tang` 字面量，朝代信息一律数据驱动
- 新动效必须受既有 `@media (prefers-reduced-motion: reduce)` 全局块兜底；初始 `opacity: 0` 的元素须在该块内置回 `opacity: 1`
- subagent 模型一律 sonnet（haiku 在本代理环境 400）
- oxlint 基线：5 条预存 warning（fast-refresh/exhaustive-deps），不得新增 error

---

### Task 1: 字体数据层——CalligraphyKind、白居易文楷、朝代字段、index.json 出生年排序

**Files:**
- Modify: `src/data/schemas.ts`（DynastyEntrySchema 加 calligraphy）
- Modify: `src/themes/types.ts`（CalligraphyKind、PoetTheme.calligraphy、DynastyInfo.calligraphy）
- Modify: `src/themes/index.ts`（CALLIGRAPHY_FONTS 加 wenkai + calligraphyFontStack helper）
- Modify: `src/themes/poets/baijuyi.ts`（calligraphy 改 wenkai）
- Modify: `data/dynasties.yaml`（tang 加 calligraphy: mashan）
- Modify: `scripts/build-data.ts`（index 按 birthYear 排序）
- Modify: `index.html`（霞鹜文楷 CDN）
- Test: `src/data/build-output.test.ts`（新建）

**Interfaces:**
- Produces:
  - `export type CalligraphyKind = 'liujian' | 'longcang' | 'mashan' | 'zhimang' | 'wenkai'`（src/themes/types.ts）
  - `DynastyInfo.calligraphy?: CalligraphyKind`
  - `export function calligraphyFontStack(key?: CalligraphyKind): string | undefined`（src/themes/index.ts）——Task 2 消费
  - `CALLIGRAPHY_KINDS`（src/data/schemas.ts，zod enum 用，与 CalligraphyKind 同序同值）

- [ ] **Step 1: 写失败测试** `src/data/build-output.test.ts`

```ts
import { describe, it, expect } from 'vitest'

// 同 PoetPage.test：直接读真实构建产物，避免维护夹具（跑前需 build-data）
const dataModules = import.meta.glob('../../public/data/*.json', { eager: true, import: 'default' }) as Record<string, unknown>

describe('构建产物纪律', () => {
  it('index.json 按出生年升序', () => {
    const index = dataModules['../../public/data/index.json'] as { birthYear: number }[]
    const years = index.map(p => p.birthYear)
    expect(years).toEqual([...years].sort((a, b) => a - b))
  })
  it('dynasties.json 各朝代声明 calligraphy（导航朝代字体数据驱动）', () => {
    const dynasties = dataModules['../../public/data/dynasties.json'] as { calligraphy?: string }[]
    for (const d of dynasties) expect(d.calligraphy).toBeTruthy()
  })
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `./node_modules/.bin/vitest run src/data/build-output.test.ts`
Expected: FAIL（当前 index.json 字母序 772 在前；dynasties.json 无 calligraphy 字段）

- [ ] **Step 3: schemas.ts 加 calligraphy 字段**

`src/data/schemas.ts` 顶部（import 之后）加：

```ts
// 与 src/themes/types.ts 的 CalligraphyKind 同序同值，改一处必同改另一处
export const CALLIGRAPHY_KINDS = ['liujian', 'longcang', 'mashan', 'zhimang', 'wenkai'] as const
```

`DynastyEntrySchema` 的 `viewBox` 行之后加：

```ts
  calligraphy: z.enum(CALLIGRAPHY_KINDS).optional(),
```

- [ ] **Step 4: themes/types.ts 定义 CalligraphyKind 并接入**

`src/themes/types.ts` 中：
- `EasterEggConfig` 之前加：`export type CalligraphyKind = 'liujian' | 'longcang' | 'mashan' | 'zhimang' | 'wenkai'`
- `PoetTheme.calligraphy` 类型由字面量联合改为 `CalligraphyKind`
- `DynastyInfo` 的 `viewBox: string` 之后加：`calligraphy?: CalligraphyKind`

- [ ] **Step 5: themes/index.ts 加字体与 helper**

`CALLIGRAPHY_FONTS` 类型改为 `Record<CalligraphyKind, string>` 并加一项 `wenkai: '"LXGW WenKai"'`（import type 处加 `CalligraphyKind`）。文件末尾加：

```ts
/** 指定书法体的完整字体栈（含楷体 fallback）；key 缺失返回 undefined，由 font-calligraphy class 兜底 */
export function calligraphyFontStack(key?: CalligraphyKind): string | undefined {
  return key ? `${CALLIGRAPHY_FONTS[key]}, "Kaiti SC", "STKaiti", "KaiTi", serif` : undefined
}
```

- [ ] **Step 6: 白居易换文楷 + 唐朝注册字体 + 排序 + CDN**

- `src/themes/poets/baijuyi.ts`：`calligraphy: 'zhimang'` → `calligraphy: 'wenkai'`
- `data/dynasties.yaml` tang 条目 `viewBox` 行后加一行：`  calligraphy: mashan`
- `scripts/build-data.ts` 在 `writeFileSync(join(root, 'public/data/index.json')...)` 之前加：`index.sort((a, b) => a.birthYear - b.birthYear)`
- `index.html` 在 Google Fonts link 之后加：

```html
    <link href="https://cdn.jsdelivr.net/npm/lxgw-wenkai-webfont@1.7.0/style.css" rel="stylesheet">
```

- [ ] **Step 7: 重建数据并跑测试**

Run: `./node_modules/.bin/tsx scripts/build-data.ts && ./node_modules/.bin/vitest run && ./node_modules/.bin/tsc -b`
Expected: build-data 全 ✓；测试全过（68 + 新增 2 条 = 70）；tsc 无输出

- [ ] **Step 8: Commit**

```bash
git add src/data/schemas.ts src/data/build-output.test.ts src/themes/types.ts src/themes/index.ts src/themes/poets/baijuyi.ts data/dynasties.yaml scripts/build-data.ts index.html public/data
git commit -m "feat: 字体数据层——白居易换霞鹜文楷、朝代 calligraphy 字段、index.json 按出生年排序

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 2: 导航锁字——诗人名各自字体、朝代 toggle/菜单/朝代印用朝代字体

**Files:**
- Modify: `src/pages/PoetPage.tsx`（nav 三处内联字体）
- Modify: `src/components/map/HeroMap.tsx`（map-dynasty-seal 用朝代名+朝代字体）
- Modify: `src/pages/OverviewPage.tsx`（dynasty-switcher 用朝代字体）
- Test: `src/pages/PoetPage.test.tsx`

**Interfaces:**
- Consumes: `calligraphyFontStack`（Task 1）、`DynastyInfo.calligraphy`（Task 1）、`poetThemes[p.theme].calligraphy`

- [ ] **Step 1: 写失败测试（追加进 PoetPage.test.tsx 的 describe 块）**

```tsx
  it('导航诗人名各自锁本人书法体，不随当前页字体', async () => {
    renderPage()
    await screen.findByText('生平年表')
    expect(screen.getByRole('link', { name: '李白' }).style.fontFamily).toContain('Liu Jian Mao Cao')
    expect(screen.getByRole('link', { name: '杜甫' }).style.fontFamily).toContain('Long Cang')
    expect(screen.getByRole('link', { name: '白居易' }).style.fontFamily).toContain('LXGW WenKai')
  })

  it('朝代 toggle 用朝代书法体（唐=马善政）', async () => {
    renderPage()
    await screen.findByText('生平年表')
    expect(screen.getByRole('button', { name: /唐 ▾/ }).style.fontFamily).toContain('Ma Shan Zheng')
  })
```

- [ ] **Step 2: 跑测试确认失败**

Run: `./node_modules/.bin/vitest run src/pages/PoetPage.test.tsx`
Expected: 两条新测试 FAIL（当前 nav 项无内联 fontFamily）

- [ ] **Step 3: PoetPage nav 三处锁字**

`src/pages/PoetPage.tsx`：
- import 行 `import { poetThemes, applyPoetTheme } from '../themes'` 改为 `import { poetThemes, applyPoetTheme, calligraphyFontStack } from '../themes'`
- 朝代 toggle button：保留 `className="poet-nav-dynasty-toggle font-calligraphy"`，加 `style={{ fontFamily: calligraphyFontStack(dynastyInfo.calligraphy) }}`
- 朝代菜单 Link 与禁用 span：各加 `style={{ fontFamily: calligraphyFontStack(d.calligraphy) }}`
- 诗人切换 Link：`className` 保留 `font-calligraphy`，加 `style={{ fontFamily: calligraphyFontStack(poetThemes[p.theme]?.calligraphy) }}`

- [ ] **Step 4: HeroMap 朝代印 + OverviewPage 朝代切换器**

`src/components/map/HeroMap.tsx`：
- import 加 `calligraphyFontStack`（from '../../themes'）、`CalligraphyKind`（from '../../themes/types'）
- `MapOverlay` props 加 `dynastyName: string` 与 `dynastyCalligraphy?: CalligraphyKind`；朝代印行改为：

```tsx
        <div className="map-dynasty-seal font-calligraphy" style={dynastyCalligraphy ? { fontFamily: calligraphyFontStack(dynastyCalligraphy) } : undefined}>{dynastyName}</div>
```

（顺带修正：`唐` 字面量改为数据驱动 dynastyName，符合 D10）
- 调用处：`<MapOverlay theme={theme} poetName={bundle.poet.name} dynastyName={dynasty.name} dynastyCalligraphy={dynasty.calligraphy} />`

`src/pages/OverviewPage.tsx`：
- import 加 `calligraphyFontStack`（现有 import 行已含 `poetThemes, resetPoetTheme, CALLIGRAPHY_FONTS`，追加）
- dynasty-switcher span 加 `style={{ fontFamily: calligraphyFontStack(d.calligraphy) }}`

- [ ] **Step 5: 跑测试确认通过**

Run: `./node_modules/.bin/vitest run && ./node_modules/.bin/tsc -b`
Expected: 全过；tsc 无输出

- [ ] **Step 6: Commit**

```bash
git add src/pages/PoetPage.tsx src/pages/PoetPage.test.tsx src/components/map/HeroMap.tsx src/pages/OverviewPage.tsx
git commit -m "feat: 导航锁字——诗人名各自书法体、朝代 toggle/菜单/朝代印用朝代字体

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 3: 地图彩蛋全删（保留名句区 5 个 + 李白翰林印章）

**Files:**
- Delete: `src/themes/easter-eggs/{CatchMoon,WestwardSuiye,StrawHutHover,BambooSway,MountainRipple,LuteNotes,LakeLevel,SnowMountain,ZenWhitespace,SlowEnding}.tsx`
- Modify: `src/themes/easter-eggs/registry.tsx`（移除 10 个 import/条目、map scope、resolvePosition）
- Modify: `src/themes/types.ts`（EasterEggConfig.type 收窄）
- Modify: `src/themes/poets/{libai,dufu,wangwei,menghaoran,baijuyi}.ts`（删 map/decoration 条目）
- Modify: `src/components/map/HeroMap.tsx`（删 renderEasterEggs 调用与 import）
- Modify: `src/themes/base.css`（删被删彩蛋的 CSS）
- Test: `src/themes/easter-eggs/registry.test.tsx`、`src/themes/themes.test.ts`

**Interfaces:**
- Produces: `renderEasterEggs(configs: EasterEggConfig[], scope: 'quote' | 'timeline'): (ReactElement | null)[]`（不再有第三参 resolvePosition）；`EasterEggConfig.type: 'quote-hover' | 'timeline'`

- [ ] **Step 1: 先改测试（失败态）**

`src/themes/easter-eggs/registry.test.tsx` 的 scope 过滤测试改为：

```tsx
  it('renderEasterEggs 按 scope 过滤', () => {
    const { container } = render(
      <div>{renderEasterEggs([
        { id: 'moon-rise', type: 'quote-hover' },
        { id: 'hanlin-seal', type: 'timeline', target: '742' },
      ], 'quote')}</div>,
    )
    expect(container.querySelectorAll('.moon-rise').length).toBe(1)
    expect(container.querySelectorAll('.hanlin-seal').length).toBe(0)
  })
```

`src/themes/themes.test.ts`：`expect(theme.easterEggs.length).toBeGreaterThanOrEqual(2)` 改为 `toBeGreaterThanOrEqual(1)`（删地图彩蛋后杜/王/孟/白各剩 1 个）。

- [ ] **Step 2: 删 5 人主题里的 map/decoration 条目**

删后各人 easterEggs 应为：
- libai：`{ id: 'hanlin-seal', type: 'timeline', target: '742' }`、`{ id: 'moon-rise', type: 'quote-hover' }`
- dufu：`{ id: 'falling-leaves', type: 'quote-hover' }`
- wangwei：`{ id: 'mist-drift', type: 'quote-hover' }`
- menghaoran：`{ id: 'falling-petals', type: 'quote-hover' }`
- baijuyi：`{ id: 'grass-sway', type: 'quote-hover' }`

- [ ] **Step 3: 删组件文件、收窄 registry 与类型**

```bash
rm src/themes/easter-eggs/{CatchMoon,WestwardSuiye,StrawHutHover,BambooSway,MountainRipple,LuteNotes,LakeLevel,SnowMountain,ZenWhitespace,SlowEnding}.tsx
```

`registry.tsx` 删 10 个 import 与 easterEggComponents 对应条目，SCOPE_MAP 改为：

```tsx
const SCOPE_MAP: Record<EasterEggConfig['type'], string> = {
  'quote-hover': 'quote',
  timeline: 'timeline',
}

export function renderEasterEggs(
  configs: EasterEggConfig[],
  scope: 'quote' | 'timeline',
) {
  return configs
    .filter(c => SCOPE_MAP[c.type] === scope)
    .map(c => {
      const C = easterEggComponents[c.id]
      return C ? <C key={c.id} target={c.target} /> : null
    })
}
```

组件签名类型改为 `Record<string, (props: { target?: string }) => ReactElement | null>`。
`src/themes/types.ts`：`EasterEggConfig.type` 收窄为 `'quote-hover' | 'timeline'`。

- [ ] **Step 4: HeroMap 删调用点**

删 `import { renderEasterEggs } from '../../themes/easter-eggs/registry'` 与 JSX 中 `{renderEasterEggs(theme.easterEggs, 'map', city => {...})}` 整块（当前 105-108 行）。

- [ ] **Step 5: base.css 删被删彩蛋的 CSS**

删除：`.lake-level` + `@keyframes lake-breathe`、`.westward-suiye`、`.straw-hut` 两条、`.bamboo-stem`/`.bamboo-sway` + `@keyframes sway`、`.rippling` + `@keyframes ripple`、`.lute-note` + `@keyframes float-note`、`.slow-dot` 系列 + `@keyframes slow-fade`。
保留：`.hanlin-seal`、`.falling-petals`/`.petal`/`fall`、`.grass-sway`/`.grass-blade`、`.moon-rise`/`.moon-disc`、`.falling-leaves`/`.leaf`/`leaf-fall`、`.mist-drift`/`.mist-band`。

- [ ] **Step 6: 全量验证**

Run: `./node_modules/.bin/vitest run && ./node_modules/.bin/tsc -b && ./node_modules/.bin/oxlint`
Expected: 全过；oxlint 仅 5 条预存 warning

- [ ] **Step 7: Commit**

```bash
git add -A
git commit -m "refactor: 地图彩蛋全删——保留名句区 5 个 hover 彩蛋与李白翰林印章，registry 收窄为 quote/timeline 双 scope

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 4: 小地图收拢到年表区（Hero 出视口 && 年表区在视口）

**Files:**
- Modify: `src/pages/PoetPage.tsx`（wrapper + 双 observer + 双 state）
- Test: `src/pages/PoetPage.test.tsx`

**Interfaces:**
- Produces: PoetPage 内部 state `heroOut`/`tlInView`（仅本文件消费）；wrapper div className `timeline-wrap`

- [ ] **Step 1: 改测试（失败态）——替换原「Hero 滚出视口渲染 MiniMap，滚回视口移除」一条**

```tsx
  it('Hero 滚出且年表区在视口内才显示 MiniMap，任一不满足即消失', async () => {
    renderPage()
    await screen.findByText('生平年表')
    const heroObs = ioInstances.find(i => i.el?.querySelector('.hero-map'))!
    const tlObs = ioInstances.find(i => i.el?.querySelector('.timeline-section'))!
    const fire = (obs: typeof heroObs, isIntersecting: boolean) =>
      act(() => { obs.cb([{ isIntersecting } as IntersectionObserverEntry], {} as IntersectionObserver) })
    // Hero 滚出但年表未入视口 → 不显示
    fire(heroObs, false)
    expect(document.querySelector('.mini-map')).toBeNull()
    // 年表入视口 → 显示
    fire(tlObs, true)
    expect(document.querySelector('.mini-map')).toBeTruthy()
    // 年表滚走（进入其人及以下）→ 消失
    fire(tlObs, false)
    expect(document.querySelector('.mini-map')).toBeNull()
    // 年表在视口但 Hero 滚回 → 不显示
    fire(tlObs, true)
    fire(heroObs, true)
    expect(document.querySelector('.mini-map')).toBeNull()
  })
```

注意：TimelineSection 自身的 reveal observer 观察的是 `ol`（`querySelector('.timeline-section')` 在 `ol` 上返回 null），与 wrapper observer 不混淆。

- [ ] **Step 2: 跑测试确认失败**

Run: `./node_modules/.bin/vitest run src/pages/PoetPage.test.tsx`
Expected: 该条 FAIL（当前实现 Hero 滚出即显示）

- [ ] **Step 3: PoetPage 双 observer 实现**

`src/pages/PoetPage.tsx` 中，将 hero observer 相关代码（当前 38-53 行）整体替换为：

```tsx
  // 小地图仅在「大地图滚出视口 && 年表区（滑块+年表）在视口内」时浮现；
  // 滚入其人及以下后年表区离口，小地图随之消失
  const heroRef = useRef<HTMLDivElement>(null)
  const tlWrapRef = useRef<HTMLDivElement>(null)
  const [heroOut, setHeroOut] = useState(false)
  const [tlInView, setTlInView] = useState(false)
  // 切换人物后回到页面顶部，并重置 MiniMap 双条件（否则残留 true，新 IO 异步回调前会闪一帧）
  useEffect(() => { window.scrollTo(0, 0); setHeroOut(false); setTlInView(false) }, [poetId])
  const loaded = state.status === 'loaded' && dynastyState.status === 'loaded'
  useEffect(() => {
    const hero = heroRef.current
    const tl = tlWrapRef.current
    if (!hero || !tl) return
    const heroObs = new IntersectionObserver(
      ([entry]) => setHeroOut(!entry.isIntersecting),
      { threshold: 0.1 },
    )
    const tlObs = new IntersectionObserver(
      ([entry]) => setTlInView(entry.isIntersecting),
      { threshold: 0 },
    )
    heroObs.observe(hero)
    tlObs.observe(tl)
    return () => { heroObs.disconnect(); tlObs.disconnect() }
  }, [loaded])
  const showMini = heroOut && tlInView
```

JSX 中 TimeSlider 与 TimelineSection 包进 wrapper：

```tsx
        <div ref={tlWrapRef} className="timeline-wrap">
          <TimeSlider min={bundle.poet.birth.year} max={bundle.poet.death.year} />
          <TimelineSection stops={bundle.poet.stops} poetId={poetId!} />
        </div>
```

- [ ] **Step 4: 跑测试确认通过**

Run: `./node_modules/.bin/vitest run src/pages/PoetPage.test.tsx`
Expected: 全过

- [ ] **Step 5: Commit**

```bash
git add src/pages/PoetPage.tsx src/pages/PoetPage.test.tsx
git commit -m "feat: 小地图收拢到年表区——Hero 出视口且年表在视口才显示，滚入其人即消失

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 5: 其人宽度 15/35/35/15 + 大标题/卷名书法体

**Files:**
- Modify: `src/themes/base.css`

**Interfaces:**
- 纯 CSS，无接口变化。jsdom 无法断言 CSS，验收靠 Task 11 playwright 截图。

- [ ] **Step 1: 改 CSS**

`src/themes/base.css`：
- `.stats { display: flex; gap: 32px; padding: 0 40px 24px; }` 改为 `.stats { display: grid; grid-template-columns: 15% 35% 35% 15%; gap: 32px; padding: 0 40px 24px; }`
- 删 `.stat { flex: 1; }`
- 768px 媒体查询内 `.stats { flex-wrap: wrap; gap: 16px; }` 改为 `.stats { grid-template-columns: 1fr 1fr; gap: 16px; }`
- `.section-title` 规则加 `font-family: var(--font-calligraphy), "Kaiti SC", "STKaiti", "KaiTi", serif;`
- `.volume-subtitle` 规则加同样的 `font-family` 一行

- [ ] **Step 2: 回归验证（DOM 未动，测试应全过）**

Run: `./node_modules/.bin/vitest run && ./node_modules/.bin/tsc -b`
Expected: 全过

- [ ] **Step 3: Commit**

```bash
git add src/themes/base.css
git commit -m "feat: 其人四组件 15/35/35/15 宽度；四个大标题与卷名改书法体

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 6: 作品时年（虚岁）+ 卷一从出生年起算

**Files:**
- Modify: `src/components/sections/volumeGrouping.ts`（birthYear 可选参）
- Modify: `src/components/sections/WorksSection.tsx`（birthYear prop + 时年渲染）
- Modify: `src/pages/PoetPage.tsx`（传 birthYear）
- Modify: `src/themes/base.css`（.album-age）
- Test: `src/components/sections/volumeGrouping.test.ts`、`src/components/sections/sections.test.tsx`

**Interfaces:**
- Produces: `groupIntoVolumes(works: Work[], birthYear?: number): Volume[]`——birthYear 传入时首卷 startYear 取 birthYear；`WorksSection props: { works: Work[]; poetId?: string; birthYear?: number }`

- [ ] **Step 1: 写失败测试**

`volumeGrouping.test.ts` 追加：

```ts
  it('传入 birthYear 时卷一从 birthYear 起算，其余卷不受影响', () => {
    const works = [work('一', 726), work('二', 727), work('三', 750)]
    const volumes = groupIntoVolumes(works, 701)
    expect(volumes[0].startYear).toBe(701)
    expect(volumes[0].endYear).toBe(727)
    expect(volumes[1].startYear).toBe(750)
  })
```

`sections.test.tsx` WorksSection 两条测试的渲染改为 `<WorksSection works={works} birthYear={701} />`，第一条断言中 `expect(screen.getByText('726—726')).toBeTruthy()` 改为 `expect(screen.getByText('701—726')).toBeTruthy()`，并追加：

```tsx
    // 虚岁 = year - birthYear + 1（与 summary stats.age 口径一致）
    expect(screen.getByText('时年 26 岁')).toBeTruthy()
```

（works 夹具：静夜思 726 年 → 726-701+1=26；将进酒 752 → 时年 52 岁。）

- [ ] **Step 2: 跑测试确认失败**

Run: `./node_modules/.bin/vitest run src/components/sections`
Expected: 新增断言 FAIL（groupIntoVolumes 无 birthYear 参；无时年元素）

- [ ] **Step 3: 实现**

`volumeGrouping.ts` 签名与首卷初始化改为：

```ts
export function groupIntoVolumes(works: Work[], birthYear?: number): Volume[] {
  if (works.length === 0) return []
  const sorted = [...works].sort((a, b) => a.year - b.year)

  const volumes: Volume[] = []
  let current: Volume = {
    title: volumeTitle(1),
    startYear: birthYear ?? sorted[0].year,
    endYear: sorted[0].year,
    works: [],
  }
```

（函数其余部分不动；docstring 补一句「birthYear 传入时卷一 startYear 取出生年」。）

`WorksSection.tsx`：
- props 改为 `{ works, poetId = 'libai', birthYear }: { works: Work[]; poetId?: string; birthYear?: number }`
- `groupIntoVolumes(works)` 改为 `groupIntoVolumes(works, birthYear)`
- album-meta 中 `.album-city` 之后加：

```tsx
                    {birthYear != null && <span className="album-age">时年 {w.year - birthYear + 1} 岁</span>}
```

`PoetPage.tsx`：`<WorksSection works={bundle.poet.works} poetId={poetId!} />` 改为 `<WorksSection works={bundle.poet.works} poetId={poetId!} birthYear={bundle.poet.birth.year} />`

`base.css` 加：`.album-age { font-size: 13px; opacity: .7; }`（置于 `.album-city` 规则后）

- [ ] **Step 4: 跑测试确认通过**

Run: `./node_modules/.bin/vitest run && ./node_modules/.bin/tsc -b`
Expected: 全过

- [ ] **Step 5: Commit**

```bash
git add src/components/sections/volumeGrouping.ts src/components/sections/volumeGrouping.test.ts src/components/sections/WorksSection.tsx src/components/sections/sections.test.tsx src/pages/PoetPage.tsx src/themes/base.css
git commit -m "feat: 作品卡标注虚岁时年；卷一从出生年起算

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 7: 首页卡片 hover 动效

**Files:**
- Modify: `src/pages/OverviewPage.tsx`（motif 包 span）
- Modify: `src/themes/base.css`

**Interfaces:**
- 新增 class `.poet-card-motif`；纯 CSS 动效，验收靠 Task 11 截图。

- [ ] **Step 1: OverviewPage 包 motif**

`OverviewPage.tsx` poet-card 内 `<MotifIcon name={t?.motifs[0] ?? 'moon'} size={48} />` 改为：

```tsx
                <span className="poet-card-motif"><MotifIcon name={t?.motifs[0] ?? 'moon'} size={48} /></span>
```

- [ ] **Step 2: base.css 加 hover 动效（置于 .poet-card 规则后）**

```css
.poet-card-motif { display: block; transition: transform .3s ease; }
.poet-card:hover { transform: translateY(-4px); box-shadow: 0 10px 28px rgba(80, 60, 30, .28); outline-color: #d4af37; }
.poet-card:hover .poet-card-motif { transform: translateY(-4px) scale(1.1); }
.poet-card:hover .poet-quote { opacity: 1; }
```

（`.poet-quote` 加 `transition: opacity .3s ease;` 并入其既有规则。）

- [ ] **Step 3: 回归验证 + Commit**

Run: `./node_modules/.bin/vitest run && ./node_modules/.bin/tsc -b`
Expected: 全过

```bash
git add src/pages/OverviewPage.tsx src/themes/base.css
git commit -m "feat: 首页卡片 hover 动效——抬升/描边晕开/motif 浮动/名句提亮

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 8: 时间轴重做——主题化定制滑块 + 拖动粒子

**Files:**
- Modify: `src/components/sections/TimeSlider.tsx`（整体重写为自定义滑块）
- Modify: `src/pages/PoetPage.tsx`（传 poetId）
- Modify: `src/themes/base.css`（删 .time-slider，加新滑块与粒子 CSS）
- Test: `src/components/sections/TimeSlider.test.tsx`（新建）

**Interfaces:**
- Produces: `TimeSlider props: { min: number; max: number; poetId?: string }`；轨道元素 `role="slider"`（aria-valuemin/max/now、tabIndex=0）；粒子 class `.tl-particle tlp-<poetId>`

- [ ] **Step 1: 写失败测试** `src/components/sections/TimeSlider.test.tsx`

```tsx
import { render, screen, fireEvent } from '@testing-library/react'
import { describe, it, expect, vi, afterEach } from 'vitest'
import { TimeSlider } from './TimeSlider'
import { PoetStateProvider } from '../../pages/poet-state'

function setup() {
  return render(
    <PoetStateProvider initialYear={701}>
      <TimeSlider min={701} max={762} poetId="libai" />
    </PoetStateProvider>,
  )
}

afterEach(() => { vi.restoreAllMocks() })

describe('TimeSlider 定制滑块', () => {
  it('方向键/Home/End 调整年份且不越界', () => {
    setup()
    const slider = screen.getByRole('slider')
    fireEvent.keyDown(slider, { key: 'ArrowRight' })
    expect(slider.getAttribute('aria-valuenow')).toBe('702')
    fireEvent.keyDown(slider, { key: 'ArrowLeft' })
    fireEvent.keyDown(slider, { key: 'ArrowLeft' })
    expect(slider.getAttribute('aria-valuenow')).toBe('701')
    fireEvent.keyDown(slider, { key: 'End' })
    expect(slider.getAttribute('aria-valuenow')).toBe('762')
    fireEvent.keyDown(slider, { key: 'ArrowRight' })
    expect(slider.getAttribute('aria-valuenow')).toBe('762')
    fireEvent.keyDown(slider, { key: 'Home' })
    expect(slider.getAttribute('aria-valuenow')).toBe('701')
  })

  it('指针按下/拖动按轨道位置换算年份并散出粒子', () => {
    const { container } = setup()
    vi.spyOn(HTMLElement.prototype, 'getBoundingClientRect').mockReturnValue(
      { left: 0, top: 0, width: 100, height: 28, right: 100, bottom: 28, x: 0, y: 0, toJSON: () => ({}) } as DOMRect,
    )
    const slider = screen.getByRole('slider')
    fireEvent.pointerDown(slider, { clientX: 50, pointerId: 1 })
    expect(slider.getAttribute('aria-valuenow')).toBe('732') // 701 + round(0.5 × 61)
    fireEvent.pointerMove(slider, { clientX: 100, pointerId: 1 })
    expect(slider.getAttribute('aria-valuenow')).toBe('762')
    expect(container.querySelectorAll('.tl-particle').length).toBeGreaterThan(0)
    expect(container.querySelector('.tlp-libai')).toBeTruthy()
    fireEvent.pointerUp(slider, { pointerId: 1 })
  })
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `./node_modules/.bin/vitest run src/components/sections/TimeSlider.test.tsx`
Expected: FAIL（无 role=slider 元素）

- [ ] **Step 3: 重写 TimeSlider.tsx**

```tsx
import { useCallback, useRef, useState, type CSSProperties, type KeyboardEvent, type PointerEvent } from 'react'
import { usePoetState } from '../../pages/poet-state'

interface Particle { id: number; dx: number }

interface TimeSliderProps { min: number; max: number; poetId?: string }

/** 主题化时间轴：水墨轨道 + 朱砂印拇指；拖动时散出诗人意象微粒子。键盘 ←/→/Home/End 可调 */
export function TimeSlider({ min, max, poetId = 'libai' }: TimeSliderProps) {
  const { year, setYear } = usePoetState()
  const trackRef = useRef<HTMLDivElement>(null)
  const [dragging, setDragging] = useState(false)
  const [particles, setParticles] = useState<Particle[]>([])
  const pidRef = useRef(0)
  const lastSpawn = useRef(0)
  const ratio = (year - min) / (max - min)

  const yearFromClientX = useCallback((clientX: number) => {
    const el = trackRef.current
    if (!el) return
    const rect = el.getBoundingClientRect()
    const r = Math.min(1, Math.max(0, (clientX - rect.left) / rect.width))
    setYear(Math.round(min + r * (max - min)))
  }, [min, max, setYear])

  const spawnParticle = () => {
    const now = Date.now()
    if (now - lastSpawn.current < 90) return
    lastSpawn.current = now
    const id = ++pidRef.current
    const dx = Math.round(Math.random() * 32 - 16)
    setParticles(ps => [...ps.slice(-11), { id, dx }])
  }
  const removeParticle = (id: number) => setParticles(ps => ps.filter(p => p.id !== id))

  const onPointerDown = (e: PointerEvent<HTMLDivElement>) => {
    e.currentTarget.setPointerCapture?.(e.pointerId)
    setDragging(true)
    yearFromClientX(e.clientX)
    spawnParticle()
  }
  const onPointerMove = (e: PointerEvent<HTMLDivElement>) => {
    if (!dragging) return
    yearFromClientX(e.clientX)
    spawnParticle()
  }
  const onPointerUp = () => setDragging(false)

  const onKeyDown = (e: KeyboardEvent<HTMLDivElement>) => {
    if (e.key === 'ArrowLeft') setYear(Math.max(min, year - 1))
    else if (e.key === 'ArrowRight') setYear(Math.min(max, year + 1))
    else if (e.key === 'Home') setYear(min)
    else if (e.key === 'End') setYear(max)
    else return
    e.preventDefault()
  }

  return (
    <div className="time-slider-wrap">
      <div
        ref={trackRef}
        className={`tl-track${dragging ? ' dragging' : ''}`}
        role="slider"
        aria-label="年份"
        aria-valuemin={min}
        aria-valuemax={max}
        aria-valuenow={year}
        tabIndex={0}
        onPointerDown={onPointerDown}
        onPointerMove={onPointerMove}
        onPointerUp={onPointerUp}
        onKeyDown={onKeyDown}
      >
        <div className="tl-fill" style={{ width: `${ratio * 100}%` }} />
        <div className="tl-thumb" style={{ left: `${ratio * 100}%` }} />
        {particles.map(p => (
          <span
            key={p.id}
            className={`tl-particle tlp-${poetId}`}
            style={{ left: `${ratio * 100}%`, '--dx': `${p.dx}px` } as CSSProperties}
            onAnimationEnd={() => removeParticle(p.id)}
          />
        ))}
      </div>
      <span className="time-slider-year font-calligraphy">{year}</span>
    </div>
  )
}
```

- [ ] **Step 4: base.css 换皮**

删 `.time-slider { flex: 1; accent-color: var(--accent); }`，加：

```css
/* ===== 主题化时间轴 ===== */
.tl-track { position: relative; flex: 1; height: 28px; cursor: pointer; touch-action: none; outline: none; }
.tl-track::before { content: ''; position: absolute; left: 0; right: 0; top: 50%; height: 2px; transform: translateY(-50%); background: var(--ink); opacity: .25; border-radius: 1px; }
.tl-track:focus-visible::before { opacity: .5; }
.tl-fill { position: absolute; left: 0; top: 50%; height: 2px; transform: translateY(-50%); background: linear-gradient(90deg, var(--accent-soft), var(--accent)); border-radius: 1px; }
.tl-thumb { position: absolute; top: 50%; width: 18px; height: 18px; transform: translate(-50%, -50%); background: var(--seal); border: 2px solid var(--paper); border-radius: 3px; box-shadow: 0 1px 4px rgba(80, 60, 30, .4); transition: left .12s ease-out, scale .15s ease; }
.tl-track.dragging .tl-thumb { transition: none; scale: 1.2; }
.tl-particle { position: absolute; top: 50%; width: 6px; height: 6px; pointer-events: none; animation: tl-float .9s ease-out forwards; }
.tlp-libai { background: var(--ink); border-radius: 50%; }
.tlp-dufu { background: #a98a5b; border-radius: 60% 40% 60% 40%; }
.tlp-wangwei { background: rgba(255, 255, 255, .85); filter: blur(2px); border-radius: 50%; }
.tlp-menghaoran { background: var(--accent-soft); border-radius: 50% 0 50% 0; }
.tlp-baijuyi { background: var(--accent); width: 2px; height: 10px; border-radius: 1px; }
@keyframes tl-float { from { opacity: .8; transform: translate(-50%, -50%); } to { opacity: 0; transform: translate(calc(-50% + var(--dx, 0px)), calc(-50% - 34px)); } }
```

- [ ] **Step 5: PoetPage 传 poetId**

`<TimeSlider min={...} max={...} />` 加 `poetId={poetId!}`。

- [ ] **Step 6: 全量验证 + Commit**

Run: `./node_modules/.bin/vitest run && ./node_modules/.bin/tsc -b && ./node_modules/.bin/oxlint`
Expected: 全过

```bash
git add src/components/sections/TimeSlider.tsx src/components/sections/TimeSlider.test.tsx src/pages/PoetPage.tsx src/themes/base.css
git commit -m "feat: 时间轴重做——朱砂印拇指/水墨轨道/主题色填充/拖动粒子/键盘可达

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 9: 作品集动效——滚动渐入 + 展卷

**Files:**
- Modify: `src/components/sections/WorksSection.tsx`（reveal observer + 卡片序号 CSS 变量）
- Modify: `src/themes/base.css`

**Interfaces:**
- 新增 class `.wk-revealed`（section 上）、CSS 变量 `--wk-i`（卡片 stagger 序号）；无组件签名变化。

- [ ] **Step 1: WorksSection 加 reveal observer**

`WorksSection.tsx`：
- import 行加 `useRef, useState`（现有 `import { useEffect } from 'react'` 改为 `import { useEffect, useRef, useState } from 'react'`），并加 `import type { CSSProperties } from 'react'`
- 组件内加（模式同 TimelineSection reveal）：

```tsx
  // 滚动进入视口后卡片按序渐入（一次性，触发后不再隐藏）
  const sectionRef = useRef<HTMLElement>(null)
  const [revealed, setRevealed] = useState(false)
  useEffect(() => {
    const el = sectionRef.current
    if (!el) return
    const observer = new IntersectionObserver(
      ([entry]) => { if (entry.isIntersecting) { setRevealed(true); observer.disconnect() } },
      { threshold: 0.05 },
    )
    observer.observe(el)
    return () => observer.disconnect()
  }, [])
```

- `<section className="works-section">` 改为 `<section ref={sectionRef} className={`works-section${revealed ? ' wk-revealed' : ''}`}>`
- 卡片跨卷连续编号：组件内 `const volumes = groupIntoVolumes(works, birthYear)` 之后加 `let cardIndex = 0`；`vol.works.map(w => {` 一行之后加 `const idx = cardIndex++`；`className={`album-card mounted-card${open ? ' open' : ''}`}` 的 article 上加 `style={{ '--wk-i': idx } as CSSProperties}`

- [ ] **Step 2: base.css 加渐入与展卷**

```css
/* ===== 作品集渐入与展卷 ===== */
.works-section .album-card { opacity: 0; transform: translateY(12px); }
.works-section.wk-revealed .album-card { animation: wk-in .55s ease-out both; animation-delay: calc(var(--wk-i, 0) * 70ms); }
@keyframes wk-in { to { opacity: 1; transform: none; } }
.album-detail { animation: unroll .45s ease-out both; }
@keyframes unroll { from { opacity: 0; clip-path: inset(0 0 100% 0); } to { opacity: 1; clip-path: inset(0 0 0 0); } }
```

reduced-motion 全局块内补一行（否则动画被禁时卡片永远 opacity 0）：

```css
  .works-section .album-card { opacity: 1 !important; transform: none !important; }
```

注意（计划勘误）：`wk-in` 动画 fill both 结束后 keyframe 终态 `transform: none` 会持续压制 `.album-card:hover` 的 `transform: translateY(-2px)`（动画填充在层叠中高于普通声明），导致既有 hover 抬升失效。必须把 `.album-card:hover` 的 `transform: translateY(-2px);` 改为独立变换属性 `translate: 0 -2px;`（translate 与 transform 是不同属性，在动画填充之上叠加生效；Task 7 的 poet-card hover 已用同法修复）。同时 `.works-section .album-card` 初始态的 `transform: translateY(12px)` 与 `@keyframes wk-in` 的 `transform: none` 保持不变（它们走的是 transform 属性，不冲突）。`.album-card` 基础规则的 `transition: transform .25s ease, ...` 中补 `translate .25s ease`，保证 hover 过渡平滑。

- [ ] **Step 3: 回归验证 + Commit**

Run: `./node_modules/.bin/vitest run && ./node_modules/.bin/tsc -b`
Expected: 全过（测试环境 IO 为 no-op，wk-revealed 不加，但 opacity 不影响 DOM 查询）

```bash
git add src/components/sections/WorksSection.tsx src/themes/base.css
git commit -m "feat: 作品集滚动渐入（IO+stagger）与详情展卷动效（clip-path 自上而下）

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 10: 名句区彩蛋按落英水准调参

**Files:**
- Modify: `src/themes/base.css`

**Interfaces:**
- 纯 CSS 参数调整，验收靠 Task 11 playwright 悬停/截图走查。

- [ ] **Step 1: 调参（基准：落英 opacity .7、6s、10px 小花瓣、局部不遮字）**

`src/themes/base.css`：
- `.moon-disc`：`width: 34px; height: 34px` 改 `26px`；`box-shadow` 透明度 `.55` 改 `.35`；`animation: moon-rise 9s` 改 `12s`；`@keyframes moon-rise` 中两处 `opacity: .9` 改 `.55`
- `.leaf`：`width: 14px; height: 9px` 改 `12px / 8px`；`opacity: .65` 改 `.5`；`animation: leaf-fall 8s` 改 `10s`
- `.mist-band`：`height: 26px` 改 `20px`；`animation: mist-drift 12s` 改 `16s`；`@keyframes mist-drift` 中两处 `opacity: .8` 改 `.6`
- `.grass-blade`：`height: 18px` 改 `14px`；加 `opacity: .6;`；`animation: grass-sway 3s` 改 `4s`

- [ ] **Step 2: 回归验证 + Commit**

Run: `./node_modules/.bin/vitest run`
Expected: 全过

```bash
git add src/themes/base.css
git commit -m "fix: 名句区彩蛋按落英水准调参——降透明度/放慢节奏/缩小尺寸，不喧宾夺主

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 11: 全量验证 + playwright 视觉走查 + 文档同步

**Files:**
- Modify: `CLAUDE.md`（特性描述与测试数同步）
- Create: `docs/screenshots/v04/*.png`（走查截图）

- [ ] **Step 1: 全量自动化验证**

Run: `./node_modules/.bin/tsx scripts/build-data.ts && ./node_modules/.bin/vitest run && ./node_modules/.bin/tsc -b && ./node_modules/.bin/oxlint`
Expected: build-data 全 ✓；测试全过；tsc 无输出；oxlint 仅 5 条预存 warning

- [ ] **Step 2: playwright 走查 11 项（dev server 任选空闲端口，如 5180）**

逐项截图存 `docs/screenshots/v04/`：
1. 小地图：Hero 在视口无小地图 → 滚到年表区出现 → 滚到其人消失（三帧）
2. 其人：四组件 15/35/35/15 比例
3. 大地图：五人页均无彩蛋残留（重点：李白当涂无月、杜甫成都无草堂、王维辋川无竹）
4. 名句区：五人各截一张彩蛋观感（月升/落木/云雾/落英/草摇）
5. 导航：诗人名各自字体混排 + 朝代 toggle 马善政 + 下拉菜单
6. 大标题与卷名书法体
7. 首页：排序（李白 701 在首）+ hover 一帧
8. 时间轴：印章拇指 + 拖动粒子一帧（playwright 模拟 pointer 拖动）
9. 作品卡：时年 N 岁 + 卷一 701— 起始
10. 作品集：渐入一帧 + 展卷展开一帧
11. 375px 窄屏：stats 两列 + 小地图 306px + 滑块可用

- [ ] **Step 3: CLAUDE.md 同步**

更新「项目一句话」特性列举与测试数（以 Step 1 实际输出为准）；遗留 Minor 中若涉本轮已清项（如 SlowEnding/CatchMoon 相关）划掉。

- [ ] **Step 4: Commit**

```bash
git add -A
git commit -m "docs+test: v0.4 走查截图与 CLAUDE.md 同步

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

## Self-Review 记录

- **Spec 覆盖**：A→Task4、B→Task5、C→Task3、D→Task10、E→Task1+2、F→Task5、G→Task1（build 排序，首页消费零改动）、H→Task7、I→Task8、J→Task6、K→Task9。全覆盖。
- **类型一致性**：`CalligraphyKind`/`calligraphyFontStack`（Task1 产，Task2 消费）、`groupIntoVolumes(works, birthYear?)`（Task6 产，WorksSection 消费）、`TimeSlider poetId`（Task8 产，PoetPage Task8 消费；Task4 JSX 注释已说明 poetId 在 Task8 才加）。
- **已知取舍**：Task5/7/9/10 为 CSS 级改动无单测，统一由 Task11 playwright 验收；TimeSlider 的 `poetId` prop 由 Task8 引入并在 Task8 接到 PoetPage（Task4 不动 TimeSlider 调用）。

