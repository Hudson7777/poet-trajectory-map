# poet-trajectory-map v0.3 六项优化 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复字体泄漏与名句选取两个 bug，完成导航/字号/印章三项增强，落地 F1/F2/F3/F8 四组动效；默认起始年改为出生年（#7）；李白/杜甫/王维补齐 quote-hover 彩蛋（#8）。

**Architecture:** #5 通过 PoetSchema 新增 `signature: string[5]` 显式声明精华名句（构建期校验一句一作），QuotesSection 改为渲染 signature；#2 新增 `resetPoetTheme` 在总览页 mount 时复位根变量，卡片用 inline fontFamily 锁定各自书法字体；动效全部走 CSS transform/opacity + SVG mask（F2 用 mask + WAAPI 实现轨迹生长，保留笔触纹理），`prefers-reduced-motion` 全局兜底。

**Tech Stack:** Vite 8 + React 19 + TS 6 + Tailwind 3 + zod 4 + vitest 4 + oxlint + d3

## Global Constraints

- **包管理命令**：`pnpm test/lint` 在无 TTY 环境下会触发 deps 检查要求清空 node_modules（ERR_PNPM_ABORTED_REMOVE_MODULES_DIR_NO_TTY）。所有验证命令直接调用二进制：`./node_modules/.bin/vitest run`、`./node_modules/.bin/tsc -b`、`./node_modules/.bin/oxlint`、`./node_modules/.bin/tsx scripts/build-data.ts`。
- **构建期校验不可绕过**：数据改动后 `./node_modules/.bin/tsx scripts/build-data.ts` 退出码必须为 0，否则禁止提交。
- **D10**：渲染层不得出现 `tang` 字面量，朝代信息一律数据驱动。
- **动效**：只用 transform/opacity/filter（GPU 友好）；全部受末尾 `prefers-reduced-motion` 全局块兜底。
- **提交信息**结尾带 `Co-Authored-By: Claude <noreply@anthropic.com>`；push 前必须经用户确认。
- 基线 commit：`44adb0d`（main，已 push）。
- 测试基线：10 文件 55 测试全过。

---

### Task 1: signature 精华名句字段（#5）

**Files:**
- Modify: `src/data/schemas.ts:49-68`（PoetSchema）
- Modify: `scripts/validate.ts:3-29`（validatePoet）
- Modify: `scripts/validate.test.ts`（夹具 + 新用例）
- Modify: `data/poets/tang/{libai,dufu,wangwei,menghaoran,baijuyi}.yaml`（各加 signature 块，位置在 `summary:` 之后、`stops:` 之前）
- Modify: `data/poets/tang/_sample.yaml`（同步加 signature 示例）
- Modify: `src/components/sections/QuotesSection.tsx`（整文件重写）
- Modify: `src/pages/PoetPage.tsx:65`（props 变更）

**Interfaces:**
- Consumes: 现有 `PoetSchema`、`Work.famous`、`validatePoet(poet, cities, dynasty): string[]`
- Produces: `Poet.signature: string[]`（恰好 5 句）；`QuotesSection({ poet, poetId }: { poet: Poet; poetId: string })`（props 从 `works: Work[]` 改为 `poet: Poet`）

- [ ] **Step 1: 写失败测试 — validate.test.ts 更新夹具并加两条用例**

basePoet 夹具的 works 扩为 5 个作品（每作一句 famous，城市只能用夹具已注册的长安/洛阳/扬州），并加 signature 字段：

```ts
const basePoet: Poet = {
  id: 'libai', name: '李白', courtesyName: '太白', dynasty: 'tang',
  birth: { year: 701, place: '碎叶' }, death: { year: 762, place: '当涂' },
  theme: 'libai',
  summary: { review: '评传', stats: { cities: 18, works: '存诗约千首', topOffice: '翰林供奉', age: 61 } },
  signature: ['床前明月光', '佳句二', '佳句三', '佳句四', '佳句五'],
  stops: [
    { year: 726, city: '扬州', event: '作《静夜思》', role: '布衣', source: '《李太白全集》卷六' },
    { year: 742, city: '长安', event: '供奉翰林', role: '翰林供奉', source: '《旧唐书·文苑传》' },
  ],
  works: [
    { title: '静夜思', year: 726, city: '扬州', genre: '诗', text: '床前明月光，疑是地上霜。举头望明月，低头思故乡。', background: '开元十四年旅寓扬州作', famous: ['床前明月光'], source: '《李太白全集》卷六' },
    { title: '作品二', year: 735, city: '洛阳', genre: '诗', text: '佳句二。其余从略。', background: '背景', famous: ['佳句二'], source: '出处' },
    { title: '作品三', year: 745, city: '长安', genre: '诗', text: '佳句三。其余从略。', background: '背景', famous: ['佳句三'], source: '出处' },
    { title: '作品四', year: 750, city: '洛阳', genre: '诗', text: '佳句四。其余从略。', background: '背景', famous: ['佳句四'], source: '出处' },
    { title: '作品五', year: 760, city: '扬州', genre: '诗', text: '佳句五。其余从略。', background: '背景', famous: ['佳句五'], source: '出处' },
  ],
}
```

describe 块内追加两条用例：

```ts
it('signature 句不属于任何作品 famous 报错', () => {
  const poet = { ...basePoet, signature: ['不存在的句子', '佳句二', '佳句三', '佳句四', '佳句五'] }
  expect(validatePoet(poet, cities, dynasty)[0]).toMatch('不属于任何作品')
})
it('signature 多句出自同一作品报错', () => {
  const poet = { ...basePoet, signature: ['床前明月光', '佳句二', '佳句二', '佳句四', '佳句五'] }
  expect(validatePoet(poet, cities, dynasty).some(e => e.includes('同一作品'))).toBe(true)
})
```

注意：既有用例中 `works: [{ ...basePoet.works[0], year: 700 }]` 等替换整数组的写法不受影响。

- [ ] **Step 2: 跑测试确认失败**

Run: `./node_modules/.bin/vitest run scripts/validate.test.ts`
Expected: FAIL — TS 报错 `signature` 不在 Poet 类型 / validatePoet 无新规则

- [ ] **Step 3: schemas.ts 加字段**

PoetSchema 中 `summary` 之后、`stops` 之前插入一行：

```ts
  signature: z.array(z.string().min(1)).length(5),
```

- [ ] **Step 4: validate.ts 加规则**

`validatePoet` 内 works 循环之后、`return errors` 之前插入：

```ts
  const sigOwners = new Set<number>()
  for (const line of poet.signature) {
    const idx = poet.works.findIndex(w => w.famous.includes(line))
    if (idx === -1) {
      errors.push(`signature 句「${line}」不属于任何作品的 famous`)
    } else if (sigOwners.has(idx)) {
      errors.push(`signature 多句出自同一作品「${poet.works[idx].title}」`)
    } else {
      sigOwners.add(idx)
    }
  }
```

- [ ] **Step 5: 跑测试确认通过**

Run: `./node_modules/.bin/vitest run scripts/validate.test.ts`
Expected: 8 个用例全过

- [ ] **Step 6: 五人 YAML 加 signature 块**

每个文件在 `summary:` 块结束后、`stops:` 之前插入（内容已定稿，逐字使用）：

libai.yaml:
```yaml
signature:
  - 床前明月光，疑是地上霜
  - 天生我材必有用，千金散尽还复来
  - 飞流直下三千尺，疑是银河落九天
  - 两岸猿声啼不住，轻舟已过万重山
  - 桃花潭水深千尺，不及汪伦送我情
```

dufu.yaml:
```yaml
signature:
  - 国破山河在，城春草木深
  - 无边落木萧萧下，不尽长江滚滚来
  - 随风潜入夜，润物细无声
  - 出师未捷身先死，长使英雄泪满襟
  - 安得广厦千万间，大庇天下寒士俱欢颜
```

wangwei.yaml:
```yaml
signature:
  - 明月松间照，清泉石上流
  - 大漠孤烟直，长河落日圆
  - 独在异乡为异客，每逢佳节倍思亲
  - 劝君更尽一杯酒，西出阳关无故人
  - 空山不见人，但闻人语响
```

menghaoran.yaml:
```yaml
signature:
  - 春眠不觉晓，处处闻啼鸟
  - 野旷天低树，江清月近人
  - 气蒸云梦泽，波撼岳阳城
  - 绿树村边合，青山郭外斜
  - 人事有代谢，往来成古今
```

baijuyi.yaml:
```yaml
signature:
  - 野火烧不尽，春风吹又生
  - 在天愿作比翼鸟，在地愿为连理枝
  - 同是天涯沦落人，相逢何必曾相识
  - 日出江花红胜火，春来江水绿如蓝
  - 几处早莺争暖树，谁家新燕啄春泥
```

_sample.yaml：在其 summary 后加 signature 示例块（5 行占位句即可）。注意：`build-data.ts:26` 以 `_` 前缀过滤此文件，PoetSchema 从不解析它——加 signature 仅为文档一致性，**不需要**为它补占位作品或保证校验通过。

- [ ] **Step 7: 构建数据验证全链路**

Run: `./node_modules/.bin/tsx scripts/build-data.ts`
Expected: 5 人全部 ✓ + index.json 写出，退出码 0（signature 校验规则生效：若某句不在对应作品 famous 中会在此步报错）

- [ ] **Step 8: QuotesSection 重写**

整文件替换为：

```tsx
import type { Poet } from '../../data/schemas'
import { poetThemes } from '../../themes'
import { MotifIcon } from '../../themes/motifs/MotifIcon'
import { renderEasterEggs } from '../../themes/easter-eggs/registry'

export function QuotesSection({ poet, poetId }: { poet: Poet; poetId: string }) {
  const theme = poetThemes[poetId] ?? poetThemes.libai
  const quotes = poet.signature.map(line => ({
    line,
    title: poet.works.find(w => w.famous.includes(line))?.title ?? '',
  }))
  return (
    <section className="quotes-section">
      <h2 className="section-title"><MotifIcon name={theme.motifs[0]} size={28} />精华名句</h2>
      <div className="quotes">
        {quotes.map(q => (
          <blockquote key={q.line} className="quote">
            <span className="quote-line font-calligraphy">{q.line}</span>
            <cite>《{q.title}》</cite>
          </blockquote>
        ))}
      </div>
      {renderEasterEggs(theme.easterEggs, 'quote')}
    </section>
  )
}
```

PoetPage.tsx:65 改为：

```tsx
<QuotesSection poet={bundle.poet} poetId={poetId!} />
```

先 grep 确认 QuotesSection 只有 PoetPage 一个调用方：`grep -rn "QuotesSection" src/`

- [ ] **Step 9: 全量验证**

Run: `./node_modules/.bin/vitest run && ./node_modules/.bin/tsc -b && ./node_modules/.bin/oxlint`
Expected: 57 测试全过、tsc 无错、oxlint 无新增 warning

- [ ] **Step 10: Commit**

```bash
git add -A
git commit -m "feat: signature 精华名句字段——schema + 构建期校验（一句一作）+ 五人定稿选句

QuotesSection 从「works flatMap slice(0,5)」改为渲染显式声明的
poet.signature，修复同作多句与知名度排序两个问题。

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 2: 字体泄漏修复 + 生卒年放大 + 总览卡片入场（#2 #3 F8）

**Files:**
- Modify: `src/themes/index.ts:20-30`（applyPoetTheme 重构 + resetPoetTheme）
- Modify: `src/themes/themes.test.ts`（加复位用例）
- Modify: `src/pages/OverviewPage.tsx`（复位 + 卡片锁字体 + 入场 delay）
- Modify: `src/themes/base.css`（.poet-years 18px、.poet-card 入场动画、reduced-motion 全局块）

**Interfaces:**
- Consumes: `poetThemes: Record<string, PoetTheme>`、`CALLIGRAPHY_FONTS`（均已存在于 themes/index.ts）
- Produces: `THEME_VARS: readonly string[]`、`resetPoetTheme(): void`（后续任务与测试依赖这两个名字）

- [ ] **Step 1: 写失败测试 — themes.test.ts 加复位用例**

在 themes.test.ts 中（顶部 import 补 `resetPoetTheme, THEME_VARS`；文件已有 applyPoetTheme、poetThemes 的 import，照其现有写法补全）追加：

```ts
it('resetPoetTheme 复位全部主题变量与 data-poet', () => {
  applyPoetTheme(poetThemes.libai, 'libai')
  resetPoetTheme()
  const el = document.documentElement
  expect(el.dataset.poet).toBeUndefined()
  for (const v of THEME_VARS) expect(el.style.getPropertyValue(v)).toBe('')
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `./node_modules/.bin/vitest run src/themes/themes.test.ts`
Expected: FAIL — resetPoetTheme / THEME_VARS 未导出

- [ ] **Step 3: themes/index.ts 实现**

将 `applyPoetTheme` 整段（现 20-30 行附近）替换为：

```ts
export const THEME_VARS = ['--accent', '--accent-soft', '--ink', '--paper', '--seal', '--font-calligraphy'] as const

export function applyPoetTheme(theme: PoetTheme, poetId: string): void {
  const el = document.documentElement
  el.dataset.poet = poetId
  const vars: Record<(typeof THEME_VARS)[number], string> = {
    '--accent': theme.accent,
    '--accent-soft': theme.accentSoft,
    '--ink': theme.inkTone,
    '--paper': theme.paperTone,
    '--seal': theme.seal,
    '--font-calligraphy': CALLIGRAPHY_FONTS[theme.calligraphy],
  }
  for (const [k, v] of Object.entries(vars)) el.style.setProperty(k, v)
}

/** 离开人物页（回总览）时复位根变量，避免上一人主题泄漏到总览页 */
export function resetPoetTheme(): void {
  const el = document.documentElement
  delete el.dataset.poet
  for (const v of THEME_VARS) el.style.removeProperty(v)
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `./node_modules/.bin/vitest run src/themes/themes.test.ts`
Expected: 全过

- [ ] **Step 5: OverviewPage 改造**

import 行改为：

```tsx
import { poetThemes, resetPoetTheme, CALLIGRAPHY_FONTS } from '../themes'
```

组件顶部（useState 之后）加：

```tsx
useEffect(() => { resetPoetTheme() }, [])
```

诗人卡片段（现 31-38 行）替换为：

```tsx
{index.map((p, i) => {
  const t = poetThemes[p.theme]
  const callig = t ? { fontFamily: `${CALLIGRAPHY_FONTS[t.calligraphy]}, "Kaiti SC", "STKaiti", "KaiTi", serif` } : undefined
  return (
    <Link key={p.id} to={`/poets/${p.dynasty}/${p.id}`} className="poet-card mounted-card" style={{ animationDelay: `${Math.min(i * 90, 600)}ms` }}>
      <MotifIcon name={t?.motifs[0] ?? 'moon'} size={48} />
      <span className="poet-name font-calligraphy" style={callig}>{p.name}</span>
      <span className="poet-years font-calligraphy" style={callig}>{p.birthYear} — {p.deathYear}</span>
      <span className="poet-quote">{p.representativeLine}</span>
    </Link>
  )
})}
```

- [ ] **Step 6: base.css 修改**

`.poet-years` 一行改为（#3，14→18px）：

```css
.poet-years { opacity: .6; font-size: 18px; }
```

`.poet-card` 一行改为（F8 入场动画）：

```css
.poet-card { display: flex; flex-direction: column; align-items: center; gap: 8px; text-decoration: none; color: var(--ink); opacity: 0; animation: card-rise .6s ease-out forwards; }
```

base.css 文件末尾追加（F8 keyframes + 全局 reduced-motion 兜底，后续任务的动效都受此块保护）：

```css
/* ===== 动效 keyframes 与 reduced-motion 兜底 ===== */
@keyframes card-rise { from { opacity: 0; transform: translateY(14px); } to { opacity: 1; transform: none; } }
@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after { animation: none !important; transition: none !important; }
  .poet-card, .timeline-section li { opacity: 1 !important; transform: none !important; }
}
```

- [ ] **Step 7: 全量验证 + 手动确认**

Run: `./node_modules/.bin/vitest run && ./node_modules/.bin/tsc -b && ./node_modules/.bin/oxlint`
Expected: 全过

手动：dev server（5174 已在跑）打开 `/poets` 确认五张卡片各用各的字体（李白刘剑毛草 vs 杜甫龙藏差异肉眼可辨），且先进人物页再返回总览字体不再串。

- [ ] **Step 8: Commit**

```bash
git add -A
git commit -m "fix: 总览页字体泄漏——resetPoetTheme 复位根变量 + 卡片 inline 锁定专属书法字体

另：生卒年 14→18px（#3）；总览卡片 stagger 入场（F8）；
新增 prefers-reduced-motion 全局兜底块。

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 3: 导航——返回总览 + 同朝诗人切换条 + 朝代切换器动态化（#1）

**Files:**
- Create: `src/components/map/usePoetIndex.ts`（数据 hook 与 usePoetBundle/useDynasty 同目录）
- Modify: `src/pages/PoetPage.tsx`（导航条 + 切人回顶）
- Modify: `src/pages/OverviewPage.tsx`（朝代切换器动态化 + 换用 usePoetIndex）
- Modify: `src/themes/base.css`（.poet-nav 一组样式）

**Interfaces:**
- Consumes: `PoetIndexEntry`（data/types.ts）、`DynastyInfo`（themes/types.ts）、`/data/dynasties.json`（数组，字段同 DynastyEntry）
- Produces: `usePoetIndex(retry?: number): PoetIndexState`（判别联合 `{status:'loading'|'error'} | {status:'loaded'; index: PoetIndexEntry[]}`）；`useDynasties(retry?: number): DynastiesState`（同构，`dynasties: DynastyInfo[]`）

- [ ] **Step 1: 创建 usePoetIndex.ts**

```ts
import { useEffect, useState } from 'react'
import type { PoetIndexEntry } from '../../data/types'

export type PoetIndexState =
  | { status: 'loading' }
  | { status: 'error' }
  | { status: 'loaded'; index: PoetIndexEntry[] }

export function usePoetIndex(retry = 0): PoetIndexState {
  const [state, setState] = useState<PoetIndexState>({ status: 'loading' })
  useEffect(() => {
    let cancelled = false
    setState({ status: 'loading' })
    fetch('/data/index.json')
      .then(r => { if (!r.ok) throw new Error(`HTTP ${r.status}`); return r.json() })
      .then((index: PoetIndexEntry[]) => { if (!cancelled) setState({ status: 'loaded', index }) })
      .catch(() => { if (!cancelled) setState({ status: 'error' }) })
    return () => { cancelled = true }
  }, [retry])
  return state
}
```

- [ ] **Step 2: useDynasty.ts 加 useDynasties**

在 `src/components/map/useDynasty.ts` 末尾追加（与 useDynasty 同构，返回整表）：

```ts
export type DynastiesState =
  | { status: 'loading' }
  | { status: 'error' }
  | { status: 'loaded'; dynasties: DynastyInfo[] }

export function useDynasties(retry = 0): DynastiesState {
  const [state, setState] = useState<DynastiesState>({ status: 'loading' })
  useEffect(() => {
    let cancelled = false
    setState({ status: 'loading' })
    fetch('/data/dynasties.json')
      .then(r => { if (!r.ok) throw new Error(`HTTP ${r.status}`); return r.json() })
      .then((dynasties: DynastyInfo[]) => { if (!cancelled) setState({ status: 'loaded', dynasties }) })
      .catch(() => { if (!cancelled) setState({ status: 'error' }) })
    return () => { cancelled = true }
  }, [retry])
  return state
}
```

- [ ] **Step 3: OverviewPage 朝代切换器动态化 + 换用 usePoetIndex**

组件内 fetch/useState 两段（现 9-16 行）替换为：

```tsx
const indexState = usePoetIndex()
const dynastiesState = useDynasties()
```

import 区最终状态：`import { useEffect } from 'react'`（删掉 useState）、删掉 `import type { PoetIndexEntry } from '../data/types'`、新增 `import { usePoetIndex } from '../components/map/usePoetIndex'` 和 `import { useDynasties } from '../components/map/useDynasty'`（useEffect 保留——Task 2 的 resetPoetTheme 调用还在用）。

朝代切换器段（现 21-26 行）替换为——硬编码的宋元明清 disabled 占位按 D10 移除，只渲染已注册朝代：

```tsx
{dynastiesState.status === 'loaded' && (
  <nav className="dynasty-switcher">
    {dynastiesState.dynasties.map(d => (
      <span key={d.id} className="dynasty active">{d.name}</span>
    ))}
  </nav>
)}
```

错误/列表段（现 27-40 行）完整替换为：

```tsx
{indexState.status === 'error' ? (
  <p className="load-error">索引加载失败，请刷新重试。</p>
) : (
  <div className="poet-wall">
    {indexState.status === 'loaded' && indexState.index.map((p, i) => {
      const t = poetThemes[p.theme]
      const callig = t ? { fontFamily: `${CALLIGRAPHY_FONTS[t.calligraphy]}, "Kaiti SC", "STKaiti", "KaiTi", serif` } : undefined
      return (
        <Link key={p.id} to={`/poets/${p.dynasty}/${p.id}`} className="poet-card mounted-card" style={{ animationDelay: `${Math.min(i * 90, 600)}ms` }}>
          <MotifIcon name={t?.motifs[0] ?? 'moon'} size={48} />
          <span className="poet-name font-calligraphy" style={callig}>{p.name}</span>
          <span className="poet-years font-calligraphy" style={callig}>{p.birthYear} — {p.deathYear}</span>
          <span className="poet-quote">{p.representativeLine}</span>
        </Link>
      )
    })}
  </div>
)}
```

- [ ] **Step 4: PoetPage 加导航条**

import 区加：

```tsx
import { Link } from 'react-router-dom'
import { usePoetIndex } from '../components/map/usePoetIndex'
```

组件内 `const dynastyState = useDynasty(dynasty!, retry)` 之后加：

```tsx
const indexState = usePoetIndex()
// 切换人物后回到页面顶部
useEffect(() => { window.scrollTo(0, 0) }, [poetId])
```

`<main className="poet-page">` 内、`<PaperTexture />` 之后插入：

```tsx
{indexState.status === 'loaded' && (
  <nav className="poet-nav">
    <Link to="/poets" className="poet-nav-back">← 返回总览</Link>
    <div className="poet-nav-switch">
      {indexState.index.filter(p => p.dynasty === dynasty).map(p => (
        <Link
          key={p.id}
          to={`/poets/${p.dynasty}/${p.id}`}
          className={`poet-nav-item font-calligraphy${p.id === poetId ? ' active' : ''}`}
        >
          {p.name}
        </Link>
      ))}
    </div>
  </nav>
)}
```

- [ ] **Step 5: base.css 加导航样式**

追加到「总览页」注释段之前（或文件末尾 keyframes 段之前）：

```css
/* ===== 人物页导航条 ===== */
.poet-nav { display: flex; align-items: center; gap: 24px; padding: 16px 40px 0; flex-wrap: wrap; }
.poet-nav-back { font-size: 15px; color: var(--ink); text-decoration: none; opacity: .65; flex-shrink: 0; }
.poet-nav-back:hover { opacity: 1; }
.poet-nav-switch { display: flex; gap: 20px; flex-wrap: wrap; }
.poet-nav-item { font-size: 20px; color: var(--ink); text-decoration: none; opacity: .55; padding-bottom: 2px; }
.poet-nav-item:hover { opacity: .85; }
.poet-nav-item.active { opacity: 1; border-bottom: 2px solid var(--seal); }
```

- [ ] **Step 6: 全量验证 + 手动确认**

Run: `./node_modules/.bin/vitest run && ./node_modules/.bin/tsc -b && ./node_modules/.bin/oxlint`
Expected: 全过

手动：人物页顶部出现「← 返回总览」+ 五人切换条，当前人有下划线；点另一人页面切主题并回顶；总览页朝代切换器只显示「唐」（宋元明清占位已移除）。

- [ ] **Step 7: Commit**

```bash
git add -A
git commit -m "feat: 导航——人物页返回总览 + 同朝诗人切换条；朝代切换器改数据驱动

新增 usePoetIndex/useDynasties 数据 hook；按 D10 移除总览页
硬编码的宋元明清 disabled 占位。

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 4: 默认起始年改为出生年（#7）

**Files:**
- Modify: `src/pages/PoetPage.tsx:53`

**Interfaces:**
- Consumes: `PoetStateProvider({ initialYear })`（poet-state.tsx:18，不变）
- Produces: 无

- [ ] **Step 1: 确认无测试依赖死亡年默认值**

Run: `grep -rn "initialYear" src/ scripts/`
Expected: 仅 poet-state.tsx 的定义与 PoetPage 的调用；如有测试断言默认卒年，同步改为出生年

- [ ] **Step 2: 改一行**

`initialYear={bundle.poet.death.year}` → `initialYear={bundle.poet.birth.year}`

- [ ] **Step 3: 验证 + 手动确认**

Run: `./node_modules/.bin/tsc -b && ./node_modules/.bin/oxlint && ./node_modules/.bin/vitest run`
手动：进入人物页，地图只落出生城市一点并飞向它（李白飞向碎叶，translateExtent clamp 属已知行为）；拖时间轴轨迹逐年生长（与 Task 7 的 F2 天然衔接）；MiniMap 朱砂点初始在出生地。

- [ ] **Step 4: Commit**

```bash
git add -A
git commit -m "feat: 默认起始年改为出生年——轨迹从人生起点随时间轴逐步生长

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 5: 地图印章放大 100%（#4）

**Files:**
- Modify: `src/themes/base.css:140-143`（印章三条规则）+ 768px 媒体查询块

**Interfaces:**
- Consumes: 无（纯 CSS）
- Produces: 无

- [ ] **Step 1: 改桌面尺寸**

```css
.map-seal-wrap { position: absolute; bottom: 20px; right: 24px; display: flex; flex-direction: column; align-items: center; gap: 12px; }
.map-name-seal { width: 72px; height: 72px; background: var(--seal); color: #fff; display: grid; grid-template-columns: 1fr 1fr; grid-template-rows: 1fr 1fr; place-items: center; font-size: 28px; line-height: 1; border-radius: 2px; box-shadow: 0 1px 3px rgba(80,60,30,.3); }
.map-dynasty-seal { width: 44px; height: 44px; border: 1px solid var(--seal); color: var(--seal); display: grid; place-items: center; font-size: 24px; border-radius: 2px; opacity: .85; }
```

- [ ] **Step 2: 加移动端断点（约 1.5×）**

现有 `@media (max-width: 768px)` 地图叠加层块（含 .map-inscription-line 那块）内追加：

```css
  .map-name-seal { width: 54px; height: 54px; font-size: 21px; }
  .map-dynasty-seal { width: 34px; height: 34px; font-size: 18px; }
```

- [ ] **Step 3: 验证 + 手动确认**

Run: `./node_modules/.bin/tsc -b && ./node_modules/.bin/oxlint`
手动：人物页右下角姓名朱印与朝代印明显放大，移动端（390 宽）不遮挡图例。

- [ ] **Step 4: Commit**

```bash
git add -A
git commit -m "style: 地图右下角姓名/朝代印章放大 100%（移动端 1.5×）

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 6: F1 年表卷轴展开动效

- [ ] **Step 0: 先补测试环境 IntersectionObserver mock（critic-claude 裁决项）**

`src/test/setup.ts` 当前只 mock 了 scrollIntoView。既有 `TimelineSection.test.tsx` 直接 render 组件，新 useEffect 里的 `new IntersectionObserver(...)` 在 jsdom 会抛 ReferenceError。在 setup.ts 追加：

```ts
if (typeof window !== 'undefined' && !('IntersectionObserver' in window)) {
  window.IntersectionObserver = class {
    observe() {}
    unobserve() {}
    disconnect() {}
    takeRecords() { return [] }
  } as unknown as typeof IntersectionObserver
}
```

Run: `./node_modules/.bin/vitest run src/components/sections/TimelineSection.test.tsx`
Expected: 现有用例仍全过（mock 是 no-op，组件未改时尚无 tl-in 行为断言）

**Files:**
- Modify: `src/components/sections/TimelineSection.tsx`（整文件重写）
- Modify: `src/themes/base.css`（timeline 三条规则改动 + keyframes）

**Interfaces:**
- Consumes: `usePoetState()`、`poetThemes`（不变）
- Produces: 无对外接口变化（props 不变）

- [ ] **Step 1: TimelineSection 重写**

```tsx
import { useEffect, useRef, useState } from 'react'
import type { Stop } from '../../data/schemas'
import { usePoetState } from '../../pages/poet-state'
import { poetThemes } from '../../themes'
import { MotifIcon } from '../../themes/motifs/MotifIcon'
import { renderEasterEggs } from '../../themes/easter-eggs/registry'

export function TimelineSection({ stops, poetId }: { stops: Stop[]; poetId: string }) {
  const { hoveredStop, setHoveredStop, setYear } = usePoetState()
  const theme = poetThemes[poetId] ?? poetThemes.libai
  // 滚动进入视口后自上而下 stagger 展开；年份推进新解锁的节点挂载时同样播放
  const olRef = useRef<HTMLOListElement>(null)
  const [revealed, setRevealed] = useState(false)
  useEffect(() => {
    const el = olRef.current
    if (!el) return
    const observer = new IntersectionObserver(
      ([entry]) => { if (entry.isIntersecting) { setRevealed(true); observer.disconnect() } },
      { threshold: 0.15 },
    )
    observer.observe(el)
    return () => observer.disconnect()
  }, [])
  return (
    <section className="timeline-section">
      <h2 className="section-title"><MotifIcon name={theme.motifs[0]} size={28} />生平年表</h2>
      <ol ref={olRef}>
        {stops.map((s, i) => (
          <li
            key={`${s.year}-${s.city}`}
            className={`${revealed ? 'tl-in ' : ''}${hoveredStop === s ? 'active' : ''}`}
            style={{ animationDelay: `${Math.min(i * 35, 420)}ms` }}
            onMouseEnter={() => setHoveredStop(s)}
            onMouseLeave={() => setHoveredStop(null)}
            onClick={() => setYear(s.year)}
          >
            <span className="tl-year font-calligraphy">{s.year}</span>
            <span className="tl-event">{s.city} · {s.event}</span>
            <span className="tl-role">{s.role}</span>
            {s.uncertain && <span className="tl-uncertain" title={s.uncertain}>存疑</span>}
          </li>
        ))}
      </ol>
      {renderEasterEggs(theme.easterEggs, 'timeline')}
    </section>
  )
}
```

- [ ] **Step 2: base.css 改动**

`.timeline-section li` 规则（现 199-202 行）改为：

```css
.timeline-section li {
  display: flex; gap: 16px; padding: 10px 12px; cursor: pointer; position: relative;
  border-left: 3px solid transparent;
  opacity: 0; transform: translateY(10px);
}
```

keyframes 段（Task 2 已在文件末尾建好的「动效 keyframes」注释块内、reduced-motion 媒体查询之前）追加：

```css
.timeline-section li.tl-in { animation: tl-reveal .5s ease-out both; }
.timeline-section li.tl-in::before { animation: tl-dot .45s ease-out both; animation-delay: inherit; }
@keyframes tl-reveal { to { opacity: 1; transform: none; } }
@keyframes tl-dot { from { transform: translateY(-50%) scale(0); } to { transform: translateY(-50%) scale(1); } }
```

注意：`tl-dot` 的 from 不设 opacity——li 自身的 tl-reveal 只声明 to 关键帧，from 隐含当前样式（opacity 0 / translateY(10px)），这是有意为之。

- [ ] **Step 3: 全量验证 + 手动确认**

Run: `./node_modules/.bin/vitest run && ./node_modules/.bin/tsc -b && ./node_modules/.bin/oxlint`
Expected: 全过

手动：人物页滚动到年表，节点自上而下依次浮现、朱砂点弹入；拖时间轴到更大年份，新解锁节点以同样动画追加；hover/点击联动不受影响。

- [ ] **Step 4: Commit**

```bash
git add -A
git commit -m "feat: 生平年表卷轴展开动效（滚动触发 stagger + 新解锁节点追加播放）

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 7: F2 轨迹生长动画（SVG mask + WAAPI）

**Files:**
- Modify: `src/components/map/Trajectory.tsx`（加 mask 生长逻辑）
- Create: `src/components/map/trajectory.test.tsx`

**Interfaces:**
- Consumes: `isIntenseBrush` 不受影响；`buildTrajectoryPath`、`visibleStops`（projection.ts）
- Produces: 无 props 变化（Trajectory 签名不变；HeroMap/MiniMap 零改动获得生长动画）

- [ ] **Step 1: 写失败测试 — trajectory.test.tsx**

jsdom 无 `getTotalLength`，测试里 mock；断言 mask 结构与生长效果存在：

```tsx
import { render } from '@testing-library/react'
import { describe, it, expect, beforeAll } from 'vitest'
import { Trajectory } from './Trajectory'
import { createProjection } from './projection'

beforeAll(() => {
  // jsdom v29 不暴露 SVGPathElement 全局——从真实元素取原型（执行时裁决 Option A）
  const sample = document.createElementNS('http://www.w3.org/2000/svg', 'path')
  Object.defineProperty(Object.getPrototypeOf(sample), 'getTotalLength', {
    configurable: true,
    value: () => 100,
  })
  // jsdom v29 无 window.matchMedia（Trajectory effect 的 reduced-motion 检查依赖）——同 Option A 模式
  window.matchMedia = ((query: string) => ({
    matches: false, media: query,
    addEventListener() {}, removeEventListener() {},
    addListener() {}, removeListener() {},
    onchange: null, dispatchEvent: () => false,
  })) as unknown as typeof window.matchMedia
})

const cities = {
  长安: { name: '长安', modernName: '西安', lon: 108.94, lat: 34.34, region: '京畿道' },
  洛阳: { name: '洛阳', modernName: '洛阳', lon: 112.45, lat: 34.62, region: '都畿道' },
}
const stops = [
  { year: 726, city: '长安', event: '事件一', role: '布衣', source: '出处' },
  { year: 742, city: '洛阳', event: '事件二', role: '官员', source: '出处' },
  // 第三站使 test 2 的 year=750→742 回拨真正缩短路径（触发 len < prevLen 直接全显分支）
  { year: 750, city: '长安', event: '事件三', role: '官员', source: '出处' },
]
const brush = { kind: 'plain' as const, colors: ['#333', '#999'] as [string, string], width: 2 }

describe('Trajectory 生长动画', () => {
  it('渲染 mask 且主线/晕线在 mask 组内', () => {
    const project = createProjection(72, 54.5, 25, 29)
    const { container } = render(
      <svg><Trajectory stops={stops} cities={cities} project={project} year={750} brush={brush} /></svg>,
    )
    const mask = container.querySelector('mask')
    expect(mask).toBeTruthy()
    expect(mask!.querySelector('path')).toBeTruthy()
    expect(container.querySelector('g[mask]')).toBeTruthy()
  })
  it('年份回拨时直接全显（不走动画分支）', () => {
    const project = createProjection(72, 54.5, 25, 29)
    const { container, rerender } = render(
      <svg><Trajectory stops={stops} cities={cities} project={project} year={750} brush={brush} /></svg>,
    )
    // 750→742：3 点路径缩为 2 点，d 变化触发 effect 重跑，len < prevLen 走直接全显分支
    rerender(<svg><Trajectory stops={stops} cities={cities} project={project} year={742} brush={brush} /></svg>)
    // querySelector 返回 Element | null，style 不在基类上——需断言（执行时裁决）
    const maskPath = container.querySelector('mask path') as (Element & { style: CSSStyleDeclaration }) | null
    expect(maskPath?.style.strokeDashoffset).toBe('0')
  })
})
```

注意：rerender 时根节点必须保持同一个 `<svg>` 包装（否则组件实例卸载、断言无意义）；jsdom 无 `Element.animate`，前进分支的 `!mask.animate` guard 会命中，两条用例都只断言可观测的 DOM 状态。回拨用例的 year 不能低于 742——更低会只剩 1 个可见 stop 触发 early return 卸载组件（执行时裁决：测试数据加第三站而非改年份，保证断言覆盖回拨分支本身）。

Run: `./node_modules/.bin/vitest run src/components/map/trajectory.test.tsx`
Expected: FAIL — 无 mask 结构

- [ ] **Step 2: Trajectory.tsx 整文件重写（含 critic-glm 裁决：Animation 实例必须 cancel）**

整文件替换为：

```tsx
import { useEffect, useId, useRef } from 'react'
import type { CityEntry, Stop } from '../../data/schemas'
import type { BrushStyle, BrushKind } from '../../themes/types'
import { buildTrajectoryPath, visibleStops, type Projection } from './projection'

interface TrajectoryProps {
  stops: Stop[]
  cities: Record<string, CityEntry>
  project: Projection
  year: number
  brush: BrushStyle
  intense?: boolean
}

const DASH_BY_KIND: Record<BrushKind, string | undefined> = {
  gold: '26 9 4 9 14 7',
  dry: '18 12 3 10',
  fade: '26 9 4 9 14 7',
  plain: undefined,
  spring: '26 9 4 9 14 7',
}

const GRADIENT_KINDS: BrushKind[] = ['gold', 'spring', 'fade']

/** 焦墨笔触（杜甫）在安史之乱（755）后转为重笔：intense 加粗加深。HeroMap 与 MiniMap 共用。 */
export function isIntenseBrush(brush: BrushStyle, year: number): boolean {
  return brush.kind === 'dry' && year >= 755
}

/** 颜色加深：intense 时主线条/晕染加深，体现杜甫安史之乱后笔力沉郁。 */
function deepen(hex: string): string {
  const m = /^#([\da-f]{6})$/i.exec(hex.trim())
  if (!m) return hex
  const n = parseInt(m[1], 16)
  const r = Math.round(((n >> 16) & 0xff) * 0.7)
  const g = Math.round(((n >> 8) & 0xff) * 0.7)
  const b = Math.round((n & 0xff) * 0.7)
  return `#${((1 << 24) + (r << 16) + (g << 8) + b).toString(16).slice(1)}`
}

export function Trajectory({ stops, cities, project, year, brush, intense = false }: TrajectoryProps) {
  const points = visibleStops(stops, year).map(s => {
    const c = cities[s.city]
    return project(c.lon, c.lat)
  })
  const d = points.length >= 2 ? buildTrajectoryPath(points, true) : ''

  // F2 轨迹生长：mask 路径 dashoffset 从「上次长度 → 0」过渡，年份前进时线条向前生长；
  // 回拨/reduced-motion 直接全显。mask 保留底层笔触纹理（枯笔/金粉不受影响）。
  // effect 重跑前必须 cancel 上一个 Animation——TimeSlider 连续拖动时每帧重跑，
  // fill:'forwards' 的 Animation 不 cancel 会持续堆积（critic-glm 裁决项）。
  const maskId = `tjg-${useId().replace(/[^a-zA-Z0-9_-]/g, '')}`
  const mainRef = useRef<SVGPathElement>(null)
  const maskRef = useRef<SVGPathElement>(null)
  const prevLen = useRef(0)
  const animRef = useRef<Animation | null>(null)
  useEffect(() => {
    const main = mainRef.current
    const mask = maskRef.current
    if (!main || !mask || typeof main.getTotalLength !== 'function') return
    const len = main.getTotalLength()
    mask.style.strokeDasharray = `${len}`
    animRef.current?.cancel()
    const reduced = window.matchMedia('(prefers-reduced-motion: reduce)').matches
    if (reduced || len <= prevLen.current || !mask.animate) {
      mask.style.strokeDashoffset = '0'
      animRef.current = null
    } else {
      animRef.current = mask.animate(
        [{ strokeDashoffset: `${len - prevLen.current}` }, { strokeDashoffset: '0' }],
        { duration: Math.min(300 + (len - prevLen.current) * 0.4, 900), easing: 'ease-out', fill: 'forwards' },
      )
    }
    prevLen.current = len
  }, [d])

  if (points.length < 2) return null
  const { kind, colors, width } = brush
  const scale = intense ? 1.4 : 1
  const mainWidth = width * scale
  const haloWidth = mainWidth * 3
  const mainColor = intense ? deepen(colors[0]) : colors[0]
  const haloColor = intense ? deepen(colors[1]) : colors[1]
  const stroke = GRADIENT_KINDS.includes(kind) ? `url(#brush-${kind}-grad)` : mainColor
  const filter = kind === 'gold' ? 'url(#brush-gold-grain)' : kind === 'dry' ? 'url(#brush-dry-flying)' : undefined
  const dash = DASH_BY_KIND[kind]
  return (
    <g className={`trajectory-group trajectory-${kind}`}>
      <mask id={maskId}>
        <path ref={maskRef} d={d} fill="none" stroke="#fff" strokeWidth={haloWidth} strokeLinecap="round" />
      </mask>
      <g mask={`url(#${maskId})`}>
        <path
          d={d}
          className={`trajectory trajectory-${kind}-halo`}
          fill="none"
          stroke={haloColor}
          strokeWidth={haloWidth}
          strokeOpacity={0.15}
          strokeLinecap="round"
        />
        <path
          ref={mainRef}
          d={d}
          className={`trajectory trajectory-${kind}`}
          fill="none"
          stroke={stroke}
          strokeWidth={mainWidth}
          strokeDasharray={dash}
          strokeLinecap="round"
          filter={filter}
        />
      </g>
    </g>
  )
}
```

- [ ] **Step 3: 跑测试确认通过 + 全量验证**

Run: `./node_modules/.bin/vitest run && ./node_modules/.bin/tsc -b && ./node_modules/.bin/oxlint`
Expected: 58 测试全过

手动：默认起始年为出生年（Task 4），进入人物页时仅出生点、无轨迹；拖时间轴前进，轨迹以毛笔行笔感向前生长；回拨直接跳变不闪；杜甫页（枯笔纹理）生长时纹理保持；MiniMap 同样生效。

- [ ] **Step 4: Commit**

```bash
git add -A
git commit -m "feat: 轨迹生长动画——SVG mask + WAAPI dashoffset，保留五人笔触纹理

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 8: F3 人物页入场水墨淡入

**Files:**
- Modify: `src/components/map/HeroMap.tsx:30-32`（题字拆字渲染）
- Modify: `src/themes/base.css`（hero-ink / insc-in / seal-stamp keyframes）

**Interfaces:**
- Consumes: `theme.inscription.line`（string）
- Produces: 无

- [ ] **Step 1: HeroMap 题字逐字渲染**

`.map-inscription` 块（MapOverlay 内）改为：

```tsx
      <div className="map-inscription">
        <span className="map-inscription-line font-calligraphy">
          {theme.inscription.line.split('').map((ch, i) => (
            <span key={i} className="insc-char" style={{ animationDelay: `${0.3 + i * 0.07}s` }}>{ch}</span>
          ))}
        </span>
        <span className="map-inscription-sub">{theme.inscription.sub}</span>
      </div>
```

- [ ] **Step 2: base.css keyframes 段追加 + 印章规则内联动画**

keyframes 段追加（hero-ink 用 scale+opacity 模拟晕染聚焦，**不用 filter: blur**——critic-glm 裁决：78vh SVG 高斯模糊每帧重绘，低端机必卡）：

```css
.hero-map .ink-map { animation: hero-ink 1.1s ease-out both; transform-origin: center; }
@keyframes hero-ink { from { opacity: 0; transform: scale(1.04); } to { opacity: 1; transform: scale(1); } }
.insc-char { display: inline-block; opacity: 0; animation: insc-in .5s ease-out both; }
@keyframes insc-in { from { opacity: 0; transform: translateY(-8px); } to { opacity: 1; transform: none; } }
@keyframes seal-stamp { from { opacity: 0; transform: scale(1.5); } to { opacity: 1; transform: scale(1); } }
```

印章动画**直接合并进既有规则**（避免同一选择器三处定义，critic-glm 建议）——Task 5 改过的两条规则各追加 animation 属性：

```css
.map-name-seal { width: 72px; height: 72px; /* …其余属性保持 Task 5 结果… */ animation: seal-stamp .45s cubic-bezier(.2,1.4,.4,1) 1.2s both; }
.map-dynasty-seal { width: 44px; height: 44px; /* …其余属性保持 Task 5 结果… */ animation: seal-stamp .45s ease-out 1.35s both; }
```

同时在 Task 2 建的 `@media (prefers-reduced-motion: reduce)` 块内追加一行（`.insc-char` 默认 opacity:0，动效被禁用后必须兜底可见）：

```css
  .insc-char { opacity: 1 !important; }
```

- [ ] **Step 3: 全量验证 + 手动确认**

Run: `./node_modules/.bin/vitest run && ./node_modules/.bin/tsc -b && ./node_modules/.bin/oxlint`
Expected: 全过

手动：进入人物页，地图从模糊晕染聚焦 → 题字逐字浮现 → 姓名朱印「盖下」→ 朝代印出现，序列约 2s；切换人物重播；reduced-motion 下全部直出。

- [ ] **Step 4: Commit**

```bash
git add -A
git commit -m "feat: 人物页入场序列——地图晕染聚焦 + 题字逐字浮现 + 朱印盖下

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 9: quote-hover 彩蛋三人补齐（#8）

**背景事实**：名句区氛围层是 quote-hover 位彩蛋，经 `renderEasterEggs(theme.easterEggs, 'quote')` 渲染（SCOPE_MAP: quote-hover→quote）。现状：孟浩然 falling-petals、白居易 grass-sway 已有；李白/杜甫/王维缺此位。本任务为三人各补一个纯 CSS 氛围彩蛋，模式完全镜像 FallingPetals/GrassSway。

**Files:**
- Create: `src/themes/easter-eggs/MoonRise.tsx`（李白·明月）
- Create: `src/themes/easter-eggs/FallingLeaves.tsx`（杜甫·落木）
- Create: `src/themes/easter-eggs/MistDrift.tsx`（王维·空山云雾）
- Modify: `src/themes/easter-eggs/registry.tsx`（import + 三行映射）
- Modify: `src/themes/poets/{libai,dufu,wangwei}.ts`（easterEggs 各加一条）
- Modify: `src/themes/base.css`（三组样式 + keyframes）

**Interfaces:**
- Consumes: `renderEasterEggs(configs, 'quote')`（QuotesSection 已调用，新彩蛋自动生效）
- Produces: 彩蛋 id `moon-rise` / `falling-leaves` / `mist-drift`（registry.test.tsx 会自动校验 id↔组件映射存在）

- [ ] **Step 1: 三个组件（模式镜像 FallingPetals.tsx）**

`src/themes/easter-eggs/MoonRise.tsx`：

```tsx
export function MoonRise() {
  return (
    <div className="moon-rise" aria-hidden="true">
      <span className="moon-disc" />
    </div>
  )
}
```

`src/themes/easter-eggs/FallingLeaves.tsx`：

```tsx
export function FallingLeaves() {
  const delays = [0, 1.1, 2.3, 3.4]
  return (
    <div className="falling-leaves" aria-hidden="true">
      {delays.map((d, i) => (
        <span key={i} className="leaf" style={{ left: `${14 + i * 22}%`, animationDelay: `${d}s` }} />
      ))}
    </div>
  )
}
```

`src/themes/easter-eggs/MistDrift.tsx`：

```tsx
export function MistDrift() {
  return (
    <div className="mist-drift" aria-hidden="true">
      <span className="mist-band" style={{ top: '30%', animationDelay: '0s' }} />
      <span className="mist-band" style={{ top: '60%', animationDelay: '4s' }} />
    </div>
  )
}
```

- [ ] **Step 2: registry.tsx 注册**

import 区加三行，easterEggComponents 映射加三行：

```tsx
'moon-rise': MoonRise,
'falling-leaves': FallingLeaves,
'mist-drift': MistDrift,
```

- [ ] **Step 3: 三人主题配置各加一条**

libai.ts easterEggs 数组加 `{ id: 'moon-rise', type: 'quote-hover' }`；dufu.ts 加 `{ id: 'falling-leaves', type: 'quote-hover' }`；wangwei.ts 加 `{ id: 'mist-drift', type: 'quote-hover' }`。

- [ ] **Step 4: base.css 追加样式**

追加到彩蛋样式区（.grass-sway 附近）：

```css
/* 李白·明月（quote-hover）：月盘从名句区底部缓缓升起 */
.moon-rise { position: absolute; inset: 0; pointer-events: none; overflow: hidden; }
.moon-disc { position: absolute; right: 12%; bottom: -30px; width: 34px; height: 34px; border-radius: 50%; background: radial-gradient(circle at 35% 35%, #fffbe8, var(--accent-soft)); box-shadow: 0 0 24px 6px rgba(255, 248, 214, .55); animation: moon-rise 9s ease-in-out infinite; }
@keyframes moon-rise { 0% { transform: translateY(24px); opacity: 0; } 25% { opacity: .9; } 50% { transform: translateY(-46px); opacity: .9; } 100% { transform: translateY(-70px); opacity: 0; } }

/* 杜甫·落木（quote-hover）：褐黄阔叶慢旋飘落，与孟浩然粉色小花瓣区分 */
.falling-leaves { position: absolute; inset: 0; pointer-events: none; overflow: hidden; }
.leaf { position: absolute; top: -14px; width: 14px; height: 9px; background: #a98a5b; border-radius: 60% 40% 60% 40%; opacity: .65; animation: leaf-fall 8s linear infinite; }
@keyframes leaf-fall { 0% { transform: translateY(0) rotate(0); } 50% { transform: translateY(130px) rotate(160deg); } 100% { transform: translateY(260px) rotate(340deg); opacity: 0; } }

/* 王维·空山云雾（quote-hover）：两道雾气横向缓移，禅意留白 */
.mist-drift { position: absolute; inset: 0; pointer-events: none; overflow: hidden; }
.mist-band { position: absolute; left: -30%; width: 60%; height: 26px; background: linear-gradient(90deg, transparent, rgba(255,255,255,.65), transparent); filter: blur(6px); animation: mist-drift 12s linear infinite; }
@keyframes mist-drift { from { transform: translateX(0); opacity: 0; } 20% { opacity: .8; } 80% { opacity: .8; } to { transform: translateX(230%); opacity: 0; } }
```

- [ ] **Step 5: 全量验证 + 手动确认**

Run: `./node_modules/.bin/vitest run && ./node_modules/.bin/tsc -b && ./node_modules/.bin/oxlint`
Expected: 全过（registry.test 自动覆盖新 id 映射；themes.test 彩蛋 ≥2 仍成立）

手动：李白/杜甫/王维页滚到精华名句区，分别看到月升、落木、云雾；孟浩然/白居易原有效果不回归；reduced-motion 下全部静止（全局块已覆盖）。

- [ ] **Step 6: Commit**

```bash
git add -A
git commit -m "feat: 补齐李白/杜甫/王维 quote-hover 彩蛋——月升、落木、空山云雾

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

## 收尾（全部 Task 完成后）

- [ ] 五人 × F1/F2/F3/F8 + 五个 quote-hover 彩蛋视觉走查（dev server 5174，playwright-cli 截图存档到 `docs/screenshots/v03/`）
- [ ] **Safari 实机走查 F2**（critic-claude 提示：Safari 对 mask 内 WAAPI dashoffset 动画可能不重栅格化）——macOS Safari 拖时间轴确认轨迹真的生长；若失效，备选改 CSS @keyframes + stroke-dashoffset 方案
- [ ] `./node_modules/.bin/vitest run && ./node_modules/.bin/tsc -b && ./node_modules/.bin/oxlint && ./node_modules/.bin/tsx scripts/build-data.ts` 最终全绿
- [ ] whole-branch review（critic）后向用户报告，经确认再 push
