# 会话交接 Prompt（poet-trajectory-map · 2026-07-31）

> **给接收会话的指令（先读这段）**：本文件由上一会话生成。行动前先验证「当前状态」：跑 `git log --oneline -5` 核对 HEAD、重跑列出的验证命令。发现漂移（文件说 X、仓库实际是 Y）→ 先向用户报告再继续。`[V]` = 生成当下已重新验证；`[?]` = 仅凭记忆，用前必须复核。

## 项目与仓库
- 本地：`~/Desktop/poet-trajectory-map`；GitHub：Hudson7777/poet-trajectory-map（私有），main 分支
- HEAD：`bf17554` [V]（`git log -1` 本次输出），main 与 origin 同步，工作区干净 [V]（`git status -sb` 本次输出 `## main...origin/main`）
- 栈：Vite 8 + React 19 + TS 6 + Tailwind 3 + d3-zoom + zod 4 + vitest 4 + oxlint + pnpm；纯静态 dist/，无后端
- 内容：唐朝 5 人 97 节点、48 城坐标表

## 本轮（2026-07-30~31）做了什么
用户报「悬浮小地图没实现、点年表年份体验糟糕」+ 四点细节优化（导航多朝代、存疑文字、图例不准、小地图太小），全部完成并提交 push。

## 当前状态（逐条带证据）
- [V] 68/68 测试通过：`./node_modules/.bin/vitest run` 本次输出尾行 `Tests  68 passed (68)`
- [V] tsc 无错：`./node_modules/.bin/tsc -b` 本次运行无任何输出
- [V] oxlint 仅 5 条预存 warning（poet-state/Trajectory/InkMap 的 fast-refresh 与 exhaustive-deps，改动前已存在）
- [V] playwright 实测通过场景：干净加载滚动出 MiniMap、点 744/730/759 年表选中态+小地图城市标注（`744 · 洛阳`）、李白→杜甫切换无闪帧、存疑点虚线无文字、图例三形状、510px 小地图、朝代下拉展开/外点收起
- **未验证区域**：Safari 实机 F2 轨迹生长动画（v0.3.0 遗留，用户自验中）；510px 小地图在窄屏/移动端的实际观感未走查（critic 警告见下）
- 测试基线注意：`src/pages/PoetPage.test.tsx` 用 `import.meta.glob` 读真实 `public/data/*.json` 做 fetch mock——**跑测试前需先 `./node_modules/.bin/tsx scripts/build-data.ts`**（CLAUDE.md 已注明）

## 关键决策与理由
- **导航多朝代**：用户三选一定「方案 C：同朝一排 + 朝代下拉」，否决目录式面板和朝代分组横排。下拉指向该朝 index 顺序首位 [V]
- **空朝代禁用**：critic 发现空 poetId 路由命中 `*` catch-all 静默重定向总览，与 D10 零代码扩展冲突 → 空朝代显示「（暂无）」span 而非 Link [V]
- **存疑地图标注**：用户明确指令「虚线本身就够了，再标字会挡内容」→ 移除 uncertain-seal 印章，tooltip 内存疑说明保留
- **小地图 510px**：用户明确指定「长宽分别扩大70%」。critic 警告 1280px 视口占 42%、375px 下比正文列宽——已向用户说明，用户保留 [V]
- **年表选中态**：selected = `visibleStops(stops, year).at(-1)`，与 MiniMap 朱砂点同函数同语义（projection.ts:31）[V]
- **hover/selected CSS**：同特异性时 hover 规则置于 selected 之后，悬停选中项也有反馈 [V]

## 走过的弯路（勿重试）
- **MiniMap「已实现却不出现」**：根因是 PoetPage 首渲染为加载屏，observer effect（deps `[]`）在 `heroRef.current=null` 时 early-return 且永不重跑。开发环境偶尔看到它是 Vite Fast Refresh remount 的假象，**不要用 dev 首开现象判断生产行为**。定位手法：MutationObserver 轮询 mini-map 存在性（80 秒观察窗），不要靠猜 observer 何时 fire
- **index.json 顺序是字母序**（baijuyi 在首）非时间序——「该朝首位文人」的测试断言勿硬编码，从 mock 数据动态取
- **playwright 标签页被串**：`location.reload()` 后 tab 内容变成别的本地应用（NoteFlow），因 dev server 或会话被抢。处理：显式 `playwright-cli open <url>` 重开再操作
- **jsdom 的 `window.scrollTo` 存在但是「未实现」桩**，调用即打警告——`!window.scrollTo` 判断无效，必须无条件覆盖（setup.ts 已改）

## 环境陷阱与纪律（项目特有，CLAUDE.md 未覆盖的新增项）
- 无 TTY 环境 `pnpm test/lint` 误清 node_modules：验证一律直调 `./node_modules/.bin/{vitest,tsc -b,oxlint,tsx}`（CLAUDE.md 已有）
- subagent 模型：haiku 在本代理环境 400，一律 sonnet 起
- 数据纪律：stops 按年份升序由 `scripts/validate.ts:7-11` 构建期强制——前端任何 `.at(-1)` 取「最新一站」的逻辑都有构建保障

## 事实源（读这些，别重新推导）
- `CLAUDE.md`（仓库根）：命令、约束 D1-D14 索引、遗留 Minor、文档地图，v0.3.2 已同步（68 测试）
- `docs/decisions.md`：D1-D14 决策
- `.superpowers/sdd/progress.md`：执行 ledger（含 v0.3.1/v0.3.2 两轮 critic 裁决记录；**该文件不入 git**）
- `docs/proofread/*.md`：五人数据校对表（内容事实源，勿动）
- `~/.claude/memory/projects/poet_trajectory_map.md`：项目记忆，2026-07-31 已更新至 v0.3.2

## 下一步（候选，按用户优先级开工）
1. Safari 实机验证 F2（SVG mask + WAAPI dashoffset；备选方案：CSS @keyframes + stroke-dashoffset 替换 WAAPI）
2. 扩朝代/扩文人（D10 零代码流程：dynasties.yaml 注册 + data/geo、data/poets 目录；朝代下拉与总览切换器均已数据驱动）
3. 观察 510px 小地图实际使用感受——用户可能回调（critic 建议的备选：`max-width: min(510px, 38vw)` 桌面响应约束、移动端收回 220-240px）
4. 视觉/交互继续迭代（改 UI 前先给方案过目）

## 待用户决定（新会话不得自行猜测）
- 510px 小地图遮挡问题是否调整（默认：保持用户指定尺寸）
- 是否打 v0.3.1/v0.3.2 tag（目前仅 push 未打 tag）
- 内容选句类决策必须用户定稿

## 协作偏好（本轮新观察）
- 三轮 critic（critic-claude）均为 CONCERN 无 REJECT，逐条采纳后复验的模式运转良好，保持
- 用户接受「先摆方案多选一」的 UI 决策方式（朝代下拉即此模式产出）
